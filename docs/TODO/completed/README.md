# Completed TODO plans

Historical plans live here so the active queue stays small. Open one plan when
its rationale is relevant; do not load this directory wholesale.

- [x] 17 — [NPC docking latch is missing from the snapshot](17-npc-dock-plan-snapshot.md)
- [x] 18 — [`draw()` advances the cockpit beam lifetime](18-beam-timer-in-step.md)
- [x] 19 — [Core rule modules still perform platform side effects](19-core-platform-side-effects.md)
- [x] 20 — [The portability gate does not follow imports](20-dependency-aware-portability.md)
- [x] 21 — [Vendor and generate the Elite-A reference catalogue](21-elite-a-reference-import.md)
- [x] 22 — [Implement the pure Elite-A combat oracle](22-elite-a-combat-oracle.md)
- [x] 23 — [Add stable ship and combat-profile identities](23-stable-ship-and-combat-profile-ids.md)
- [x] 24 — [Replace approximate geometry with all 38 Elite-A designs](24-exact-elite-a-geometry.md)
- [x] 25 — [Bring the complete Elite-A ship roster into runtime](25-complete-elite-a-runtime-roster.md)
- [x] 26 — [Use exact player lasers, NPC energy and defence](26-player-lasers-and-npc-energy.md)
- [x] 27 — [Use 255-point player defence and clean NPC lasers](27-player-defence-and-npc-lasers.md)
- [x] 28 — [Audit secondary damage and remove mixed units](28-secondary-damage-and-mixed-units.md)
- [x] 29 — [Rebaseline simulations, training and campaign combat](29-combat-training-and-balance-rebaseline.md)
- [x] 30 — [Add the permanent Elite-A alignment gate](30-elite-a-damage-alignment-gate.md)
- [x] 31 — [Give the setup panel a shape, and fence the career switch](31-trainer-setup-panel-hierarchy.md)
- [x] 32 — [Make choosing a brain a real choice](32-trainer-brain-choice-is-legible.md)
- [x] 33 — [Tell the pilot they are in an exercise, and how it is going](33-exercise-hud.md)
- [x] 34 — [Put the turret tell in the report](34-report-shows-how-they-flew.md)
- [x] 35 — [Compare two records without leaving the room](35-compare-two-records.md)
- [x] 36 — [Start the exercise where the pilot can see it](36-exercise-opening-geometry.md)
- [x] 37 — [Do not throw away a tap that arrived in a busy frame](37-input-taps-are-not-lost.md)
- [x] 38 — [The console still shows four energy banks](38-energy-reads-as-one-bank.md)
- [x] 39 — [Make the wave ramp keep getting harder](39-waves-keep-getting-harder.md)
- [x] 40 — [Named saves you make, and autosaves you can fall back to](40-named-save-files.md)
- [x] 41 — [Name the opposition, not the file](41-name-the-opposition-not-the-file.md)
- [x] 42 — [They stop shooting when you get close](42-they-stop-shooting-when-you-close.md)
- [x] 43 — [Loading or importing a save eats a career's checkpoint](43-career-identity-has-two-homes.md)
- [x] 44 — [A full store deletes a pre-slots commander](44-a-full-store-deletes-a-legacy-commander.md)
- [x] 45 — [“NEW COMMANDER” does nothing](45-new-commander-does-nothing.md)
- [x] 46 — [Docking rerolls the board a restore just loaded](46-docking-rerolls-the-board-a-restore-just-loaded.md)
- [x] 47 — [The trainer credits no damage for ordnance](47-the-trainer-credits-no-damage-for-ordnance.md)
- [x] 48 — [Shields freeze at exactly the low-energy mark](48-shields-freeze-at-exactly-the-low-energy-mark.md)
- [x] 49 — [Guards that do not guard](49-guards-that-do-not-guard.md)
- [x] 50 — [Key bindings have six homes](50-key-bindings-have-six-homes.md)
- [x] 51 — [The market estimate lies](51-the-market-estimate-is-wrong.md)
- [x] 52 — [Say true things](52-say-true-things.md)
- [x] 53 — [Delete the legacy save migration](53-delete-the-legacy-save-migration.md)
- [x] 54 — [Import can write a save the shelf cannot read](54-import-can-write-an-unreadable-save.md)
- [x] 55 — [Make saving and loading legible](55-make-saving-legible.md)
- [x] 56 — [A player plays a commander rather than a career](56-a-player-plays-a-commander-rather-than-a-career.md)
- [x] 57 — [Ship only what ships](57-ship-only-what-ships.md)
- [x] 58 — [A generator writes a longer description for every world](58-a-generator-writes-a-longer-description-for-every-world.md)
- [x] 59 — [The encyclopaedia puts all 256 worlds on a public page](59-the-encyclopaedia-puts-all-256-worlds-on-a-public-page.md)
- [x] 60 — [The playtest agent strands itself after two or three legs](60-the-playtest-agent-strands-itself.md)
- [x] 61 — [Promote or delete the attack-run candidate](61-decide-the-attack-run-candidate.md)
- [x] 62 — [Missiles do not exist in training](62-missiles-do-not-exist-in-training.md)
- [x] 63 — [A training target's shields never come back](63-shields-never-come-back-in-training.md)
- [x] 64 — [One resolver, so the trainer and the game cannot drift](64-one-fire-resolver.md)
- [x] 65 — [The selection rule rewards a defender for not fighting](65-the-selection-rule-rewards-a-defender-for-not-fighting.md)
- [x] 66 — [The pass aims where you were](66-the-pass-aims-where-you-were.md)
- [x] 67 — [A short attack run does not fly, so the rhythm settles near nine seconds](67-a-short-attack-run-does-not-fly-so-the-rhythm-settles-near-nine-seconds.md)
- [x] 68 — [Every hostile in the game flies one pattern](68-every-hostile-in-the-game-flies-one-pattern.md) — Chris, 2026-08-03: *"we should think of these scripted behaviours as different strategies that an NPC can pick from randomly."* Every hostile flew ONE pattern, which is a good pattern and a learnable one. `src/game/tactic-choice.ts` rolls a tactic per ship and `describeFlight` names it, so the trainer's SPENT ITS TIME column reads TWO WORDS — the tactic, then the leg. **Restored on 2026-08-16 by docs/TODO/165.** The plan landed and was then deleted rather than moved to this directory, so seven live citations named nothing. This line is written from the plan and from the code that runs, and not from a recorded outcome: the document carries none.
- [x] 69 — [The setup panel says “HULL (0)”](69-the-panel-says-hull-zero.md)
- [x] 75 — [A gang never knows it is losing](75-a-gang-never-knows-it-is-losing.md)
- [x] 76 — [No test covers wingman avoidance](76-no-test-covers-wingman-avoidance.md)
- [x] 77 — [A brain-flown ship is “evading” forever](77-a-brain-flown-ship-is-evading-forever.md)
- [x] 81 — [The live-brain picker is dead code](81-live-picker-cannot-name-attack-run.md) — written on 2026-08-09, deleted rather than archived, and **restored and landed on 2026-08-16 inside docs/TODO/165**, on Chris's call. Everything it reported was still true seven days later: `LIVE_BRAIN_IDS`, `liveBrainSelection` and `liveBrainId` had no caller in `src/` at all, and only tests kept them alive. The four members are gone. **`selectionForBrain` STAYS, and the plan is wrong to list it** — `combat-sim.ts:923` calls it. **One test PINNED the defect rather than caught it**: it asserted that exactly one id failed to round-trip, and named `attack-run` as that one, so the collision had a check holding it steady. **The live rule that survives the six deleted checks** is *every selection the game can be put in flies the policy the report names*, which builds its list from `selectionForBrain` now. Four other test files named the deleted members and none of the four is about the picker; `test/combat-sim-compare.test.ts` was still handing the screen `liveBrain` and `selectLiveBrain`, **two properties that are not on that context type at all**. **Two sentinels and their table are the same defect and are REPORTED rather than deleted**: `AS_SHIPPED`, `AS_THE_GAME_FLIES` and `SENTINEL_NAMES` are read by `brainName`'s fallback alone, which every live caller misses. That is `internal-claims.mjs`'s own rule — report a member with no caller, do not delete it in the same pass. One number carried two plans, and the other is in `retired/`. 4,715 assertions, down 6.
- [x] 83 — [No test covers the one-warhead-in-the-air cap](83-no-test-covers-the-one-warhead-in-the-air-cap.md)
- [x] 87 — [Three parity checks assert `f(x) === f(x)`](87-three-checks-that-restate-their-own-implementation.md)
- [x] 88 — [The flight readout still quotes two stale words](88-the-readout-still-quotes-two-stale-words.md) — `flownBy` is re-decided every step, so the readout names the flight that moved the ship instead of the branch it entered or a phase it never ran
- [x] 90 — [Every constant gets one home](90-every-constant-gets-one-home.md)
- [x] 93 — [One colour has fourteen homes in the code](93-one-colour-has-fourteen-homes-in-the-code.md) — 90's
      other half: colour. `src/palette.ts` owns it, `src/palette.css` is
      generated from it, `npm run palette:check` holds the tree to it
- [x] 94 — [One door parses a save, or refuses the whole of it](94-one-door-parses-a-save-or-refuses-the-whole-of-it.md)
- [x] 96 — [The character label drives nothing in the world yet](96-the-character-label-drives-nothing-yet.md) — phase 2 of Character: a name is now one more thing a pirate can see, and the rock hermit's credential up to the Dodgy rung and shut door at it. Landed on the campaign, not on a flight — `DISREPUTE_HEAT`, `COURTESY_RATE` and `HERMIT_FAVOUR` are unflown starting values and anything that plays wrong becomes a GitHub issue
- [x] 97 — [Nobody can tell which build the live site runs](97-nobody-can-tell-which-build-the-live-site-runs.md) — closed GitHub issues #12–14
- [x] 98 — [Nothing asserts that a fight keeps its human shape](98-nothing-asserts-that-a-fight-keeps-its-human-shape.md)
- [x] 99 — [`npm run survivability` cannot run](99-survivability-cannot-run.md)
- [x] 100 — [Who flies the combat computer?](100-who-flies-the-combat-computer.md)
- [x] 102 — [Retired brains still load in two places](102-two-things-still-load-the-retired-brains.md)
- [x] 103 — [Train tools still name retired brains](103-the-train-tools-still-name-the-retired-brains.md)
- [x] 104 — [An agent must read all 58 constants files to find one](104-an-agent-must-read-all-58-constants-files-to-find-one.md)
- [x] 105 — [The cycle orchestrator runs the loop as a deterministic script](105-the-cycle-orchestrator.md)
- [x] 106 — [A new pilot must see the instructions](106-new-pilot-instructions.md)
- [x] 92 — [The lead marker assumes every target is a freighter](92-the-lead-marker-assumes-a-freighter.md)
- [x] 108 — [A pod is not a canister](108-a-pod-is-not-a-canister.md)
- [x] 109 — [The bulletin board offers no passenger berth](109-the-bulletin-board-offers-no-passenger-berth.md)
- [x] 110 — [Smuggling runs price the scan](110-smuggling-runs-price-the-scan.md)
- [x] 112 — [The consignment goes back](112-the-consignment-goes-back.md)
- [x] 113 — [The contract bills you for what you cannot hand back](113-the-contract-bills-you-for-what-you-cannot-hand-back.md) — closed GitHub #17
- [x] 111 — [The chart shows the danger it already reports](111-the-chart-shows-the-danger-it-reports.md) — GitHub #10, first slice
- [x] 114 — [The chart shows where the trade is](114-the-chart-shows-where-the-trade-is.md) — closed GitHub #10
- [x] 115 — [Point at a lane and it tells you what it carries](115-point-at-a-lane-and-it-tells-you-what-it-carries.md) — hover and cursor detail, lanes faded by traffic
- [x] 116 — [A loaded save jumps on its own](116-a-loaded-save-jumps-on-its-own.md) — restore puts the hyperspace countdown back at rest, and the in-flight ring will not write one down
- [x] 117 — [The galaxy was trading before you arrived](117-the-galaxy-was-trading-before-you-arrived.md) — a new career inherits 30 days of trade, and a galactic jump arrives in a galaxy of its own
- [x] 119 — [A constant that names nothing, and the exports around it](119-a-constant-that-names-nothing.md) — `BRAIN_HANDOVER_RANGE` and the prose asserting a handover no pilot makes; two dead functions, two dead re-exports, eleven narrowed visibilities
- [x] 120 — [The port marker says LINED UP before the roll matches](120-the-port-marker-says-lined-up-before-the-roll-matches.md) — three states off the slot's own two tests, decided in the model: green now means the dock test would pass
- [x] 121 — [The test mode that has no door](121-the-test-mode-that-has-no-door.md) — closed GitHub #18. `GameState.cheat` was built, saved and unreachable; ⇧T at the station is the door and twenty levers are behind it — the commander's fuel, credits, legal status and Character, and a FIT-OUT that takes equipment OFF, which no shop in the game can. The jump stops asking about fuel. A career that switches it on is marked for good. A SPAWN key was built for M3 and removed at Chris's word; the plan doc records the amendment
- [x] 124 — [There is no way out of a flight](124-no-way-out-of-a-flight.md) — asked directly by Chris. **Q** in the cockpit gives up the flight and puts you back at the station autosave you launched from, asking first. It costs what dying costs and lands where dying lands, which is why it is offered to every pilot rather than gated behind test mode

- [x] 122 — [The police scan arrives with no warning](122-the-scan-arrives-with-no-warning.md) — closed GitHub #20. The scan was a silent proximity test that resolved into a verdict, so there was no decision to make; **POLICE PATROL CLOSING** is the 1,800-wide window before it, and it can only warn about a ship inside scanner range. The flight's finding — being scanned makes you an Offender, and police hunt Fugitives, so the Viper that read your hold flies on — was made legible rather than rebalanced: a second line names the record and who does come, assembled from `lawTakesInterest`, which `isHostileToPlayer` now also spends. **O** is the dump you can aim, most valuable CONTRABAND first, because `dumpCargo`'s pirate-priced ordering puts Slaves 14th of 17
- [x] 123 — [You cannot buy off the law](123-you-cannot-buy-off-the-law.md) — closed GitHub #21. `satisfied` already ended a ship's interest in you for every role and `isHostileToPlayer` already asked it before the role; only the offer was missing, and only the comment said "pirate". **L** in the cockpit offers the police ship in front of you money: inside 122's window the scan simply does not happen (`policeScanned` latches with no `raiseLegal`), priced at half of what the evidence fetches at `VALUE_PER_TONNE`; a Viper already shooting breaks off for four times the fine for your rung, per ship, and you are exactly as Fugitive afterwards as you were. Neither half touches the record and both cost `DISREPUTE_BRIBE` — refusals included, because the deed is the asking. M3 shipped rather than being cut: the offer is refused on a seeded roll that falls as your name is made, and a refusal provokes the man you tried to buy

- [x] 128 — [The cockpit never says what to press](128-the-cockpit-never-says-what-to-press.md) — asked directly by Chris. `POLICE PATROL CLOSING` named a problem and no answer, and the answers lived in the `?` guide, the manual and the README — three places nobody is looking while a Viper closes. A prompt line of its own under the console now says what a key can do about what is happening RIGHT NOW, priced: `L PAY 141.0 Cr AND YOUR NAME` · `O DUMP THE EVIDENCE`, and five more moments — the E.C.M., the pirate's tonne, the beacon, the docking computer — each gated on the refusal the key itself would give. `game/prompts.ts` is pure and carries a `Command`, never a letter; the label is applied at the edge, so rebinding rewrites the prompt. Prompts are STATE, not messages: they are true while the situation is, ranked by urgency and capped at two, because three is a menu. M3 turned the feature on its neighbour — `test/key-prose.test.ts` scans `src/game/` and fails on a key spelled out in a message, which found two more offenders than the plan expected and gave `ordnanceMessage` an `offer` so a rule module could stop naming U. Invariant 9 covers prose now

- [x] 127 — [The game hands a survivor over without asking](127-the-game-hands-a-survivor-over-without-asking.md) — closed GitHub #22. Docking filed anyone you scooped out of a capsule with station medical in the same breath as resetting your shields, so the one genuinely moral act in the game cost nothing and meant nothing. It is a forced choice now, on a screen ESC cannot dismiss: **M** hands them over for nothing, **V** sells them at the station's own Slaves quote for `DISREPUTE_SLAVE_SALE` and an Offender's record, **L** takes half the sale to let them walk for a quarter of the name. Neither dirty answer touches `cargo` — a survivor is not stock (docs/TODO/108) — and the sale's fallout is `saleFallout`'s rule, not a copy. It found one thing on the way: `raiseLegal` had never been reachable from the station, and a docked ship sits inside `DEFENCE_RANGE`, so the sale scrambled Vipers at a ship on the pad
- [x] 126 — [The docking computer turns the ship without flying it](126-the-docking-computer-flies-by-fiat.md) — found in flight by Chris. It wrote `player.quaternion` through a shortest-arc slerp: a pivot about an axis no stick can produce, no rates for any instrument to read, and a turn limit of its own rather than the hull's. It produces a `FlightDemand` now. The hard part was that ONE stick has TWO jobs — bank to turn, and line the wings up with a letterbox on a spinning hull — and the answer is the slot's own tolerance as a budget: the turn may bank anywhere inside `ROLL_TOLERANCE * DC_SLOT_MARGIN` of lined up, in a window that opens to a half turn far out. Two rival laws crossfaded instead was measured rolling at a steady 1 rad/s into the hull. `DC_TURN_RATE` and `DC_THROTTLE_GAIN` are gone rather than retuned, and `npm run dock-probe` is the 320-approach measurement that says so: 320/320 docked before and after, median 15.2s → 16.8s, worst 36.3s → 30.6s

- [x] 118 — [Two files write out the bloom and the pixel-ratio clamp](118-two-files-write-out-the-bloom-and-the-pixel-ratio-clamp.md) — the last entry on docs/TODO/93's list, and the reason it sat in the backlog was a policy question rather than a colour: silencing the catalogue's duplicate-value warning the tidy way looked like it meant handing `@rule` ids to nineteen unrelated constants. It does not — the checker is diff-scoped, so it warns once about the new constant and asks nothing of the others — and the answer recorded here is that `@rule` means "these equal values must stay free to move apart" and should not be spent on every number that happens to be 0.5. `constants/render.ts` is the home; the cockpit, the two dev pages and the encyclopaedia's chart read it, and a new scan over every file in `src/` fails if the literals come back

- [x] 130 — [The record moves and nothing says so](130-the-record-moves-and-nothing-says-so.md) — found while flying docs/TODO/129's tests, and the third sighting of one defect: `raiseLegal` said `LEGAL STATUS: FUGITIVE` into a console `callStationDefence` took away three lines later, so the most expensive thing that can happen to a commander short of dying was never read by anybody. `raiseLegal` is the one home of what a moved record says now, and it says `recordVerdict` — the same fact plus who is coming — queued, behind the launch, which queues too. The `LEGAL STATUS:` string is deleted rather than moved, and so are the copies the scan and the survivor sale had each written out for themselves. The console has a running order: **what you did → what the sky did about it → where you now stand**, and only a MOVE speaks, because every laser hit on a trader reaches this. `test/record-line.test.ts` flies it, and the thing it taught is that a watcher reading the console cannot count: two identical verdicts never CHANGE the line, so a duplicate is only visible in `session.queued` and in whether the console ever falls quiet

- [x] 131 — [A person is worth less than the record selling one files](131-a-person-is-worth-less-than-the-record-selling-one-files.md) — docs/TODO/127's own finding, promoted after measurement made the flight unnecessary. Across galaxies 1, 2 and 8 a tonne of Slaves is 2–16 Cr, median 8–10; a sale filed an Offender record costing `OFFENDER_FINE` (25 Cr) to clear and `DISREPUTE_SLAVE_SALE` (40) on top. The dirty answer was not merely untempting, it was **strictly dominated** — never correct at any market in any galaxy — so 127's forced choice had three branches and two answers. `SURVIVOR_SALE_TONNES` is what a person is worth on that row, and 4 is bracketed by two measured rules rather than chosen: below it the deed cannot cover its own cleanup at a median market (3 fails in two galaxies of three), at 13 the CHEAPEST market clears the fine too and where you are docked stops deciding anything. An unflown number belongs at the bottom of its bracket, and the test asserts minimality so the next person can see the value was reasoned. It answers ONE of the playtest's four questions, and only because that one was arithmetic; `DISREPUTE_BRIBE` and 96's three still need a flight

- [x] 129 — [Your name changes and nothing says so](129-your-name-changes-in-silence.md) — asked by Chris. Eight deeds and the decay moved a Character score the game never mentioned, so a pilot could reach Notorious without being told once: the same defect 122 fixed for the scan, one rung along. `rungCrossed`/`characterVerdict` (game/character.ts) are the one rule — two `characterName` calls, so the line cannot promise a rung the status screen does not show — and only a CROSSING speaks, so a hidden score never becomes a running commentary. M1 also replaced `scanVerdictTimer` with `session.queued`, because a scan owes the console TWO lines and one slot silently ate the second. M2 (`DISREPUTE_BRIBE`) closed later with docs/TODO/132, by measurement rather than the flight it was waiting for
- [x] 132 — [The unflown values get anchors instead of a flight](132-the-unflown-values-are-anchored-not-flown.md) — Chris: "do not block things on my playtest, use sensible default values". Four numbers were holding the queue open, and each already had an anchor in the codebase that nobody had gone and looked for. `DISREPUTE_HEAT` stopped being a knob: its own doc said it meant "as interesting as a fat sale", which names `SALE_NOTORIETY_MAX` — both were 0.5, typed separately and free to drift, so it is that constant now and cannot disagree with its own rationale. `DISREPUTE_BRIBE` was never waiting on a flight but on arithmetic: a deed's weight means nothing except against the rate that forgives it, and over all 1,686 jumps galaxy 1 allows a median jump forgives 6 disrepute against the bribe's 12 — one bad afternoon washes off in two quiet jumps, a habit reaches Dodgy by the fourth. `COURTESY_RATE` and `HERMIT_FAVOUR` kept their values and gained the gates their mechanisms lacked: the stick must outweigh the carrot, and the hermit's welcome must stay a perk rather than a wholesale channel. No value moved on taste; the queue is empty and nothing waits on a flight

- [x] 133 — [The docking computer plays the C64's own arrangement](133-c64-docking-music.md) — Chris supplied a frame-by-frame decode of the Commodore 64 Elite music binary, which is exactly the material `src/music.ts` used to decline in capitals: the waltz is Strauss and public domain, Julie Dunn's 1985 transcription of it for three SID voices is not, and decoding it into JSON changes the container and nothing else. The conflict was raised before anything was written and Chris chose to use it, so the work has two halves. The claim: `README.md` no longer says the repo contains no assets from Elite, and `reference/danube/README.md` sets out what the arrangement is, that the reversal was deliberate and whose decision it was. The engine: `src/music.ts` is a SID player now — three permanent voice chains and a six-event cutoff sweep, nine nodes rather than three thousand — and the release rule is REVERSED, hanging past the gate as the chip does rather than fitting inside the note as a legato score needs. Verified by rendering against the supplied reference player offline: correlation 1.000000, max sample difference 0.0005. `src/music-score.ts`, the arrangement this project wrote to avoid the question, was deleted rather than left unreachable

- [x] 134 — [The docking computer rolls at nothing](134-the-docking-computer-rolls-at-nothing.md) — closed GitHub #23, and Chris's guess on the issue ("a similar control problem to the combat computer") was right in the family and sharper than it read. The turn measured its roll against `nose × heading`, whose length is the sine of the off-nose angle: it VANISHES exactly when the controller succeeds, and a vanishing vector still has a direction. So a ship dead on the gate heading rolled hard over and back every 0.45s at ±1.3 rad/s, chasing numerical residue at full stick, with `rollErrorTo`'s guard six orders of magnitude too small to notice. It survived docs/TODO/126 because `dock-probe` scored docked, seconds and scrapes and all three were fine — docking well and flying well are different claims and only the first had a number, so M1 taught the probe to count roll reversals BEFORE anything changed. The fix needed two gates, not the one the plan argued for: `DC_TURN_FADE_ANGLE` ramps the turn's claim in over the off-nose angle, AND the slot does not ask for the axis until the run phase commits — the fade alone just swaps chasing a degenerate axis for chasing a letterbox on a spinning hull 1,500 units away, which is a roll that never stops and a pitch plane that sweeps round under `pitchOnto` until the nose hunts too. Median approach: 17 roll reversals → 8, worst 29 → 15, 320/320 docked either way, scrapes 3 → 1. The fade angle was chosen on the flat middle of a sweep and confirmed on a second, independent grid, because the worst-case and fall-back columns named a different winner on each grid and were therefore noise

- [x] 135 — [The approach is one frame deep](135-the-approach-is-one-frame-deep.md) — found by Chris flying 134: *"we pitch violently, and then pitch back up. And then pitch violently again. How do we plan our trajectory and paths?"* The premise was generous — there is no trajectory, path or lookahead anywhere in the docking computer, and nothing avoids anything. `planDocking` returns one aim point per frame and `dockingSticks` steers at it. The gate was a point to arrive AT rather than pass THROUGH, so a ship converging from the side cut the corner, ended up INSIDE the gate, and on committing had its aim teleport from a point behind it to one in front: 220 single-frame heading jumps over 20 degrees, all within 200 units of that point, the worst 162 degrees — and `pitchOnto` saturates at 20 degrees of error, so what the pilot got was full stick, an overshoot and a ring back. The fix is a lookahead the ship EARNS by being lined up, and both halves were measured: aiming abeam with no lookahead changes nothing (the aim then sits 44 units away and is as ill-conditioned as the arrival it replaced), and leading unconditionally fixes the jump and ruins the docking (335 scrapes against 1, cutting the corner into the hull). Worst jump 162° → 4.3°, and every other column came with it: median 16.8s → 15.0s, scrapes 1 → 0, and fall-backs 4 → 0, which retired 134's open backlog entry — runs were giving up BECAUSE the aim teleported. M2 built `npm run dock-traffic`, the measurement that had never existed: `dock-probe` clears the sky every frame, so every "320/320 docked" this project has ever quoted was flown in an empty one. It found its own fixture bug first (a fresh world is empty for minutes — traders arrive on a ~50-160s timer from 22,000 units out — so the sky has to be pre-rolled) and then the number: **one non-fatal collision in eighty approaches**. Fatal ones are counted apart because a ram that kills runs `Combat.destroy`, which files an `offence` against a commander who did not fly the manoeuvre; that path exists and was not reached. M3, avoidance, is argued AGAINST on that number and left as Chris's call, with the design bias recorded — wait, do not swerve

- [x] 136 — [The approach has never been flown from behind](136-the-approach-has-never-been-flown-from-behind.md) — found by Chris flying 135: *"I'm still seeing extreme pitch. You can recreate by placing the ship on the other side of the station pointing towards it."* M1 was the finding and it was about the MEASUREMENT: every approach `dock-probe` had ever flown started on the slot side, so "504/504 docked" meant "docked from in front", and the two worst defects in the approach lived in a hemisphere nothing looked at. On the real sweep the plan reversed through **180 degrees on 223 of 504 approaches**, because the stand-off branch pushed radially — no progress at all from astern — and had no hysteresis. M2–M3 replaced the whole reactive shape with a PATH (`game/dock-path.ts`) and a follower that aims one lookahead along it: a fixed stand-off funnel holding the gate distance from a quarter turn round to astern and diving to the slot inside it, maximum'd with the ship's own way in through where it actually is, marched rather than solved. **504/504 docked, median 19.4s → 16.4s, worst 38.6s → 31.6s, 1 scrape → 0, and NO approach's plan jumps more than 20 degrees against 223 (worst 180.0° → 1.1°).** Chris's own case went from 28 seconds and ten full-authority pitch reversals to 16 seconds and one; `npm run dock-traffic` went from one non-fatal collision in eighty approaches to none. Two rounds of him FLYING it landed on top of the item and became two new probe columns, because nothing existing could see either: how far off the slot's axis the ship is still POINTING as it goes through the letterbox — 13.6° when he said *"it feels quite tight into the slot so the angle seems a bit too much"*, 5.4° once the approach gained a straight RUN IN at three fifths of the gate — and how far its WINGS are off the letterbox, 20.4° when he asked *"how closely do we match the rotation?"*, 7.5° once the roll was handed over a gate distance out instead of at the corridor edge, against 37° of tolerance. Three things the measurements decided against what looked right, and all three are recorded where they were spent: a path re-rooted on the ship every frame has no restoring force and spirals into the hull (353 scrapes, all from 900 units out) because a lookahead follower flies INSIDE a curve by half a lookahead of radius per radian; the lookahead has to be clamped by what is LEFT of the path, or a ship inside the last one aims at the station's centre, holds its bearing instead of closing it and arrives at the hull face; slowing for the bend — which reads perfectly — buys nothing, because the ring it aims at is a reversal a second whatever the speed; the run in has to FOLLOW a ship already on it, or the whole path sits behind that ship and the aim lands astern; and where the ship IS on the path has to be blended rather than chosen, because a nearest-point projection swaps for a ship deep inside the funnel (the aim slid a quarter of the way round the station in one frame, 21°) while switching on the run latch steps it 149 units on every approach (12°). WHAT IT DID NOT FIX, measured and handed to 137: the roll hunts around any bank it holds, identically before and after this item and on a dead-straight approach, so it is `dockingSticks` and not the plan. M4, replanning round traffic, was named to keep the shape open and is not built

- [x] 137 — [The roll rings around any bank it holds](137-the-roll-rings-around-any-bank-it-holds.md) — the residue of #23, and the last thing in the docking computer that still moved when nothing asked it to. `dockingSticks` overshot every bank it was given and hunted round it at about a reversal a second, because a proportional ask (`steerStick`) driving a rate ramp (`PLAYER_FLIGHT.rateRamp`) is a second-order loop with no damping term at all — a gain of 7.1/s against a 0.242s ramp is a damping ratio of 0.38, which is 25% overshoot and a ring, and the disturbance never stops because the ship rolls past the bank, its pitch plane sweeps with it and the heading error moves in its own frame. **`DC_ROLL_LEAD` asks for where the roll error WILL be, a tenth of a second ahead at the rate already being flown**, and over the 504-approach sweep the median approach goes from 18 roll reversals and 1.9 turns swept to **12 and 0.9**, with a second independent 336-approach grid agreeing on every median. THE SECOND HALF is what the ring had been hiding: damping it alone took the wings at the letterbox from 7.5 degrees off the slot to 8.8, and that was the fix WORKING — 7.5 was never a controller sitting 7.5 degrees off, it was a **±40-degree swing** (traced frame by frame down the corridor) sampled wherever the letterbox caught it, and a ship that sits where it is asked sits at whatever `DC_SLOT_MARGIN` allows. So the margin became measurable for the first time and was re-chosen with it, 0.5 → **0.30**: the wings arrive at 3.1, 3.8, 4.3, 5.0 and 5.5 degrees for margins of 0.25 to 0.35, a straight line through the constant, and 0.30 is the KNEE rather than the floor — below it the median keeps falling while the worst case blows out past where it started (33.5° at 0.25, 34.3° at 0.28, against 30.0° before the item) on both grids, which is the turn being refused a bank it genuinely needs. **Landed at 4.4 degrees median and 13.8 worst, from 7.5 and 30.0**, with docked, scrapes, seconds and the plan's own jump column all unmoved and traffic still clean. What it cost is the NOSE, and it is the same bank spending itself twice: 5.4° → 6.3° in the median, 12.4° → 9.7° at worst. A FEEDFORWARD FOR THE STATION'S SPIN was built and measured away — the letterbox turns at 0.26 rad/s forever, so a proportional law can only hold the wings on it by holding a standing 1.8° error, and crediting the roll the slot itself asks for removes the lead's share of that exactly on a fixture and is worth **0.1 degrees** in flight, which does not pay for a field on the plan and a gate for the phase. `game/docking-sticks.ts` is a file now, along the seam the constants and tests already had, and `test/dock-path.test.ts` is the mirror of that split.

- [x] 140 — [The day is the one cost nothing shows](140-the-day-is-the-one-cost-nothing-shows.md) — GitHub #24, and Chris's call to put it at the head of the queue. A jump spends fuel, money and days. The chart priced two of them and never named the third. `commander.day` was always right, and it was on screen only where nobody consults it. **M1 put it where the pilot already looks**: `Elapsed: N days` on the COMMANDER screen between fuel and cash, `· DAY N` on the docked menu, and a fourth topbar span, so the day ticks in flight where a jump happens. It also cost a defect that was waiting: `test/ui.test.ts` left a half-built `document` global behind, and the first real `Game` in a later file died on it. **M2 priced one jump in days on both charts.** `oneJumpDays` owns the rule and one private `daysTerm` owns the words, so two charts cannot word one cost differently. A third special case was not in the plan, and a shipped galaxy holds it: galaxies 4, 5 and 8 each put a pair of systems on one chart point, so the term must read `1 DAY`. **M3 priced the journey one jump cannot make.** `src/galaxy/route.ts` is a Dijkstra over full-tank edges, because fuel costs money and no days, and both charts now say `EST 16 DAYS, 4 JUMPS` where they said nothing. Three things came out of it that the plan did not have. The map is far sparser than the estimate (6.6 neighbours a system, not 15), so a search costs 0.2 ms. Some destinations have no route at all, and shipped galaxies hold them: nothing is within a full tank of Oresrati in galaxy 8, and galaxy 7 splits 229 systems from 27. And the numbers are large — Sori is 89 days and 21 jumps from Lave — so the verdict reads TOO FAR across most of the chart, honestly. `test/route.test.ts` checks the ANSWER and not the algorithm, by the shortest-path certificate, and a lost tie-break on jumps is caught by the symmetry check alone. **M4 answers the contract question.** An amber diamond marks a world you owe a delivery to, on both charts, and the info line ends `DUE IN 6 DAYS · 3 DAYS AWAY`. `src/game/contract-eta.ts` is pure and owns the words and the colour. It takes the whole commander rather than a day number, which closes the trap the plan recorded: the living galaxy's day is also in scope in both painters, and it lags a long save by up to 60 days a load. **The plan's red rule was one day out, and the code follows the game instead**: settlement calls a delivery late when `c.day > k.deadlineDay`, so arrival ON the deadline day pays, and the rule is `daysAway > daysLeft`. Five more states are reachable in play and the plan worded none of them: `DUE TODAY`, `OVERDUE BY 2 DAYS`, `YOU ARE HERE`, `NO ROUTE`, and `+1 MORE` for two jobs at one world. The marker needed a recording canvas, and there was none — `inert-dom.ts` gives a painter a context that returns undefined, so a mark drawn nowhere and a mark never drawn looked the same. `test/screen-capture.ts` gained `captureCanvas`. **Flown by Chris on 2026-08-12 and confirmed good** — *"display is good"* — which answers the two questions no probe can: whether a fourth topbar span reads well in flight, and whether the contract marker crowds a chart that already draws eight things.

- [x] 141 — [Nobody reading a rule can ask what it meant](141-nobody-reading-a-rule-can-ask-what-it-meant.md) — Chris's call on 2026-08-12: rewrite the technical documentation in ASD-STE100 Simplified Technical English, as a house style rather than a one-time pass, and with **no gate**. STE is a controlled language built for one reader — somebody who must act on a written instruction and cannot ask what it meant — and that reader is the one this repository has. **M1 took the four rule docs**, which are read at the top of every session and cited by the code **107 times by invariant number**. All 15 invariants keep their numbers and their claims, the 107 citations still resolve, and the seven doc paths that `src/`, `test/` and `tools/` name are unchanged at the same counts. **M2 wrote the convention down, and second on purpose**: a `## Prose` section in `CLAUDE.md` with the countable rules as a table, the surfaces it covers, and the seven kinds of text it never touches — code, an exact command or error string, anything quoted from a person, a record of what was decided or measured, `README.md`'s opening, the player-facing pages, and `CATALOG.md` by hand. The one-line rule in `## Style` is deleted rather than kept in step. **M3 took the six reference docs**, `DAMAGE-PATHS.md` last, whose 25-row inventory was not edited at all — only the prose around it — because `test/damage-paths.test.ts` reads that table. **M4 took the constants' prose at its source**, all 61 files of `src/constants/`, then `npm run generate:constants`: the catalogue still reports 374 exports and 54 rule ids, which is what says the pass changed prose and not rules. Four things came out of it that the plan did not have. A dated report pins its own text, so `JAMESON-TRIALS.md`'s note now says what is true rather than "left as written". `TACTICS` had no JSDoc, because `constants:check` is diff-scoped and an export can sit undocumented until somebody edits its file. Two quotations attributed to `CLAUDE.md` are no longer in it, and are left verbatim because a quotation rewritten is falsified. And `docs/PROCESS.md` still cites a "step 3" that went with the cycle orchestrator. `npm run check` green throughout — 4255 passed, 0 failed. STE compliance itself is not measured, exactly as the plan said it would not be.
- [x] 139 — [Nothing the galaxy sends can get through the shield](139-nothing-the-galaxy-sends-gets-through-the-shield.md) — found by Chris flying it: *"is our shield and energy recharging too fast — the laser hits from pirates don't seem to do much damage — or they aren't very accurate at shooting..."* All three guesses were live, and the first was provable without a flight: **fourteen of the seventeen pirate builds could never strip a shield face**, because their best case — point blank, capped hit chance, never out of the gate — was under `SHIELD_REGEN`'s 8.925 points a second. **M1 measured time on aim, because nothing did.** `npm run aim-probe` replaced a comment that said "about 5%" from memory: the shipped pilot is inside its gate for 11.9% of a one-on-one knife fight, 26.5% with four in the sky, and 55% of a chase, and **the two fights fail differently** — 85.6° off her when she turns to fight, and lined up but out of reach when she runs. No row in the grid landed more laser than one face put back. **M2 cut `SHIELD_REGEN_FRACTION` 0.035 → 0.012**, a face in 83 seconds instead of 28.6, on a sweep confirmed at two sample sizes on two seed grids. The value is the knee and not the floor, and it is the highest one that clears the rule `test/role-variants.test.ts` now pins: no build the galaxy sends may be one a face simply outruns. A tier-2 gang of three reaches ENERGY LOW in **49.5% of fights against 34.0%** and destroys her in **35.5% against 15.0%**; `npm run survivability`'s destroyed column left zero for the first time. Nothing else moved — not the bank, not a damage number, not how anybody flies. **M3 decided the aim does NOT move, and one measurement is why.** The probe's fourth table splits its own aim column by the leg the ship was flying, and the 85.6° turns out to be four legs: `closing` 42.6% at 64.1°, `on your six` 32.1% at 37.0°, `extending` 13.7% at 142.0° and `passing` 11.6% at 102.3°. **A quarter of the fight points the nose away BY DESIGN** — that is the attack run's pass-and-extend working — and neither leg that wants the nose ON her is a pilot that cannot point: `on your six` is pure pursuit with no lead, and `closing` aims beside her on purpose so the run clears the hull. So a wider `NPC_FIRE_GATE` and a "fixed" aim both mean deleting a designed behaviour. The regen had already bought both decisions the plan records, a perfect gun is a further factor of four to thirteen, `pursuit.ts` is shared with the player's own combat computer, and invariant 5 makes the flight model the world every pilot was fitted in. The lever is recorded for whoever wants it: the missing lead in `pursuitAim`. **M4** re-baselined `docs/COMBAT-SIM.md`; its other half is 138's roster probe, which does not exist yet and is baselined in the world this left behind — the whole reason 139 went first in the queue.

- [x] 138 — [Every system in every galaxy flies the same roster](138-every-system-flies-the-same-roster.md) — raised by Chris after 137 flew, and it waited behind 139 because its baseline had to be taken against a fight that could bite. Elite-A did not ship one roster: it shipped **23**, the files `S.A` to `S.W`, and it chose between them on arrival. Harmless imported all of it and then collapsed the dimension, so a Krait was the same Krait in all eight galaxies and all 2,048 systems. **M1 took the baseline and the tables were flat** — `npm run roster-probe` walks every system of every galaxy, prices each reception against a Cobra Mk III through `npcWeaponByte`, `npcLaserDamageToPlayer` and `npcBestCasePerSecond`, and all eight galaxies reported the same row: 17 designs, 17 builds, 13.2 damage a hit. **M2 recovered the rule as a pure function.** `game/blueprint-set.ts` takes a system, a 1-based galaxy, two random bits and an optional override, and returns one of the 23 letters; the bit table came from bbcelite's deep dives, which are a fourth source because the vendored pack carries no selection metadata at all. Bit 0 turned out to be a rule that had been running for years — the tech-level test that picks the Dodo station over the Coriolis — so it is READ from `galaxy/tech.ts` and not restated, and moving `DODO_TECH_LEVEL` moves the set with it. **The pack corroborates both overrides without being made to**: set G is the only set the slot table ever fills slot 31 in, which is the Constrictor's own slot, and sets C and D are the two that carry Thargoids. **M3 wired the choice in**, and three of the four things it found were not in the plan. The number M1 told it to raise could not rise, because 17 was already the ceiling — every pirate design Harmless files is filed by some set, and a career census is a union. What the choice buys is the opposite shape: **a band of 4.4 designs per arrival where there were 17, over 23 distinct pirate rosters.** Every pirate in the game comes through a threat tier rather than through the band, so `pirateSpecForTier` had to take the roster in force or the one band this item is about would have gone untouched; on the path the game actually spawns on, tier 1 rose, tier 2 fell 2.9%, tier 0 fell 7.3%, and every minimum and maximum is unchanged because no build moved. M1's worry that a light set would breach the regeneration floor cannot happen, and the reason is structural rather than lucky: the set narrows which designs turn up and never touches a build. **Twelve of the 23 sets empty a threat tier and four bands are empty somewhere**, and one rule covers all of it — where a set files nothing for a job, the full roster answers, because a set does not get to downgrade the threat rule. **M4 gave the two released overrides a caller.** `missionBlueprintOverride` in `game/missions.ts` owns both mission facts, `chooseBlueprintSet` names the override, `enterWitchspace` chooses a set at all, and limbo is asked FIRST — a mis-jump on the hunting leg is still limbo, and the Constrictor waits in a system rather than between two. Four things came out of it that the plan did not have: an override must not draw, and the Thargoid ambush is what says so (an unconditional draw takes it from three Thargoids to two); an override raised at a dock takes effect at the next arrival, which is the arrival-only rule working rather than a hole in it; witch-space picks its tech branch from the system you jumped FROM, because a mis-jump does not move `commander.systemIndex`; and 21 of the 23 sets file no Thargoid, so limbo had been flying the empty-band fallback until now.

- [x] 142 — [Every explosion is in the cockpit](142-every-explosion-is-in-the-cockpit.md) — GitHub #25: *"An explosion far away has the same volume as an explosion next to you."* True, and wider than reported. A `SoundEvent` carried no place at all, so `Game.playSound` called `sfx[e.name]()` with nothing to scale by and every world sound played at the gain it was written with. **M1 gave the event a place** — `at?: THREE.Vector3`, type-only three import so `sounds.ts` stays headless — set at five emitters and left off every cockpit sound. `Combat.wreck` takes its position BEFORE it despawns the ship, which is the one way that seam can fail invisibly. **M2 put the curve in `audio.ts` and the range in the constants home**, which is the same split `sfx.countdown(n)` made when the pitch arithmetic left the world step: the Game measures geometry, the synth decides what it is worth. `AUDIBLE_RANGE` is `SCANNER_RANGE` as an expression rather than a second 6,000, because the claim is that you hear what you could see; the falloff is `(1 - d/R)²`, chosen against a true inverse-square law that would put a wreck at 300 units a hundredth as loud as one on the hull and leave a fight inaudible long before it stopped being dangerous. **M3 is Chris's own addition on the day, and it is the sky in stereo.** `viewRight` sits beside `viewDirection` in `views.ts`, and the ear turns with the VIEW rather than with the hull — the same argument that file already makes for the gun, so a ship on the left of the screen is on the left in rear view too. The docking waltz had built every hard part already: `a.createStereoPanner?.()` with a straight-through fallback, and a fixture that records panners. `STEREO_WIDTH` is 0.7 rather than a hard 1.0, because a hard-panned voice vanishes on a mono speaker and reads as a fault in the headphones. **His question about the NPC laser changed the design, and the code answered it rather than a judgement**: he asked whether a bolt should be judged by where it was fired from or by how close it passes, and `heard('enemyLaser')` turns out to be pushed only in the branch where the shot is at the PLAYER — an NPC firing on another NPC draws a tracer and says nothing — so the beam always ends on the hull and attenuating by the shooter's range would have muted the one sound that always happens next to the pilot. It is **placed and never attenuated**: three categories, not two. **Eight things came out of it that the plan did not have.** The largest is that **no test had ever played a noise** — the fake `AudioContext` had no `createBuffer`, so `explosion`, `hit`, `damage`, `ecm`, `bomb`, `hyperspace` and `tunnel` every one threw on the first call under it, because all 32 sounds `test/audio.test.ts` names are built from `tone`. The envelope's floor decides two things at once and is written once: a voice scaled below the level the envelope decays TO would ramp upwards over its own length, so `env` returns null and nothing is built, which is also why a wreck beyond earshot costs no oscillator. The zero-distance guard is defensive rather than live and the plan claimed otherwise — `hitPlayer` carries the MISSILE's position, inside `MISSILE_HIT_RANGE` and never equal — and the comment beside it says so now. `CARGO_LOSS_CHANCE` and `THARGOID_FIRE_RATE` needed `@rule` ids, because `separateRules` only counts two constants independent when both carry one and a third constant had arrived on 0.7. The domain check scores a constant's own domain from the module HEADER alone, so `src/constants/audio.ts` names the reach and the width in its first two lines. And two comments were wrong before this item touched them: `world-step.ts` said *"A tone, in hertz"* above `heard`, describing a `beep` helper deleted when every sound took a name, and `Combat.fire` worked out where a bolt struck the station AFTER it asked for the bang. `test/sound-place.test.ts` is 30 assertions and all five gates were proven able to fail. **Flown by Chris on headphones the same day**, which is the only thing that could answer whether a fight still sounds dangerous once the far half goes quiet — and neither `AUDIBLE_RANGE` nor `STEREO_WIDTH` had to move
- [x] 143 — [The counter never says which tonnes a contract claims](143-the-counter-never-says-which-tonnes-a-contract-claims.md) — GitHub #26: *"I think the contract cargo can be sold - I'm not sure this should be allowed."* It can, it is allowed on purpose, and the triage answered the question the issue asks by measurement rather than by judgement: over **138 freight jobs from 86 home systems of galaxy 1**, selling the consignment at the DEAREST price the galaxy can roll and letting the job expire beat delivering it **0 times**, and the closest it ever came was delivering paying 2.24 times the sale. So the temptation is priced and the rule stays. **What was actually wrong is that the pilot could not see it.** The market screen read `c.cargo[i]` and nothing else, `MarketScreen.sell` knew nothing about contracts at all, and the only screen that names a consignment is the bulletin board — which **a rock hermit does not have**, the tunnel opening the market and nothing else. That is why Chris found it at a hermit: you sell 10 tonnes of Food and learn three jumps later that five of them were the job. **M1 put the answer on the row.** `consignedTonnes` sits beside `berthTonnes` in `commander.ts` and is DERIVED for the reason that one gives — a stored copy can disagree with the contract list, and the screen, the sale and settlement must not hold three answers. It sums a `cargo` and a `smuggle` job only: a berth is not cargo, and a bounty leaves its `commodity` field unread, so a bounty must not mark the Food row. `renderMarket` marks the `IN HOLD` cell `10t · 5 CONSIGNED` in `--hud-amber`, which is the colour `renderContracts` already spends on an illicit job and for the same reason — flagged, not disguised — so no colour is coined. The suffix reports the JOB rather than a share of the hold, because goods are fungible and the hold keeps no per-contract provenance: 15t of Food against a 5t consignment says 5, and the other ten are hers. **M2 made the sale ask once.** The first sell key on a marked row spends itself on `5T CONSIGNED — PRESS V AGAIN TO SELL` and the second one sells; one row is armed at a time, and a moved cursor, a click, a purchase and leaving the screen all take the arming down. `SELL ALL` arms the same way, so the fastest way to void a contract is not one keystroke. It is a warning and not a refusal, and no hermit-only door was built: a rule with two homes is what `CLAUDE.md` forbids, and the hermit is the worst place to put one. **Four things came out of it that the plan did not have.** The arming lives in `input` rather than in `sell`, and the plan's warning names the wrong caller — `test/playtest.js` never calls `sell` at all, it empties the hold itself and only ever calls `g.buyCargo`; the path that does reach `sell` is `Game.sellCargo`, and keeping the arming out of `sell` leaves it open. A row already sold down to nothing still carries the mark, `- · 5 CONSIGNED`, because the reader reports the job — and that is the last warning there is, since `settleContracts` bills exactly those tonnes at the door. The warning and the receipt take the same `ctx.message` door, so a test that counts what the screen said counts the receipt too. And the message is allowed to spell a key because `test/key-prose.test.ts` excludes `src/game/screens/`, where a screen reads a raw code and prints its own keyline. The triage also recorded one asymmetry nobody has to find again: **a shortfall is billed at the destination and free everywhere else** — the same missing tonnes arriving LATE cost nothing, and settlement cannot see whether the hold is short because it was sold or because it was robbed. `test/consigned-hold.test.ts` is 38 assertions, and all three gates were shown to fail.
- [x] 144 — [A standing order has nowhere to live](144-a-standing-order-has-nowhere-to-live.md) — GitHub #27, found by Chris flying it: he met the Constrictor and did not remember ever being briefed for it. A **standing order** is an obligation that outlives the moment it is announced, and the game has two kinds — a signed contract, and the Navy mission. They shared one amber line under the station header, and `Station.missionText` returned the first contract and reached the Navy line only when the commander held none. So two jobs hid the Constrictor's system completely, and the only other place it was ever written was a transmission said one time for five seconds that **does not name the system at all**. The triage found four things the report did not have. A contract has a durable home in the bulletin board's ACCEPTED table, so **the Navy mission was the only standing order in the game with no screen**; that home is a station, so in flight neither kind was readable; both charts marked a contract destination and never the Navy target; and a census of every `say(...)` line in `station.ts`, `world-step.ts` and `game.ts` found only two that announce a standing order, so the wider half of the issue is a rule rather than a sweep. **M1 asked the two kinds the same question for the first time** — `game/orders.ts`, which restates no rule: a contract's words come from `describeContract` and the mission's from `missionOrderLine`. The Navy sorts above the work because a board re-offers a job every day and the Navy briefs a commander one time. **M2 gave standing orders a screen**, on ⇧I at the station and in the cockpit, above the plain I in both tables — the cockpit half is the one that matters, because the pilot who met the Constrictor was forty light years from the station that briefed her. It exists when it is empty: a screen that hides itself is a screen a player learns not to open. **M3 made the menu line one entry per KIND** and gave a `StationEvent` message a `Command` to point at, rendered at the edge through `keyPointer`, so the transmission ends `— ⇧I MISSIONS` and no sentence in `src/game/` spells a letter. **M4 put the Navy target on both charts**, in the diamond docs/TODO/140 built, with a contract answering first where one world carries both — a contract has a deadline and the mission does not. **M5 is invariant 16**: a standing order has a screen, a console line never holds the only copy of one, and a surface that carries orders never drops one kind for another. Two gates hold it as behaviour — a walk of the mission machine end to end, and a matrix over both mission states and four contract counts asserting that the number of kinds HELD equals the number of kinds NAMED. **Six things came out of it that the plan did not have**, and the largest is a second defect: **the gun warning deleted the transmission it explains**. Both were pushed with `say` in the same frame and `showMessage` takes the console, so a commander with the wrong gun never saw that the Navy had called — she got the warning with no announcement in front of it. It is queued now, which is the rule `session.ts` already states. `test/key-prose.test.ts` could not see it, because neither line spells a key. Also: four mission readers became `missionLeg`, because four let a caller assemble three states the machine cannot produce; `openStatus` became `openReadingScreen`; and the README's two hand-written key tables are a gate that caught ⇧I's omission on the first run. **M6 is the flight, and it found a defect every gate had missed — in the key this plan chose.** Clicking the `⇧I MISSIONS` row opened the COMMANDER STATUS screen: a menu row is a click target, `dockedMenuHtml` writes `data-key` without the modifier, so the click injected a plain I and the plain entry answered. **A shifted MENU ROW cannot keep invariant 13's promise at all**, and ⇧T only ever dodged it by being a keyline caption rather than a row. The key is **`R`** now, the only plain letter free in both tables, and the rule is a gate: `test/key-help.test.ts` presses every docked row through the click path and asserts it asks for the command the row advertises. Nothing could see it before, because `test/standing-orders.test.ts` asserted the BINDING resolved — which was true — and no test joined the binding to the HTML the row renders to. **Flown at Leesti on 2026-08-13** with 16 kills, a beam laser and two contracts held: the station line carries both kinds and wraps to two lines, the console says `INCOMING NAVY TRANSMISSION — ⇧I MISSIONS` with the gun warning queued behind it, `R` opens the screen from the station and from the cockpit, the row is clickable, and the short range chart draws the diamond on Lave reading `NAVY MISSION · 3 DAYS AWAY`. **No save was put at risk**: the browser held three real careers, the page was switched to the harness namespace before any docking (invariant 3), and all seven player keys were byte-identical afterwards. `npm run check` passes, and every gate added was shown to fail.
- [x] 145 — [Two kinds of work need two screens](145-two-kinds-of-work-need-two-screens.md) — Chris, on reading 144: *"we have contracts and we have missions. We can see our contracts on the contracts screen, but they are also now on the missions screen... should they be there?"* Split by kind. **MISSIONS is the Navy's alone** and reads `missionLeg`, so one leg needs no table; the test asserts the split rather than assuming it, and a contract held at the same time must NOT appear. **CONTRACTS opens in flight, on ⇧C** — `atStation` splits the screen, the ACCEPTED half travels because what she owes is true wherever she is, and the board does not because `state.contractOffers` is the LAST station's work. The accept key is REFUSED rather than hidden: a control that is invisible and still spends money is the dead-control failure inverted. ⇧C is safe where ⇧I was not, because 144 M6's trap belongs to `dockedMenuHtml` and the cockpit has no rows. **One name each**: the headings were `STANDING ORDERS` and `LEESTI STATION BULLETIN BOARD` while the rows said MISSIONS and CONTRACTS. **Five things came out of it that the plan did not have.** The largest is that **M4's premise was wrong and the plan was the thing at fault** — it called the board "a second, independent rendering" that could word a job differently, and it could not, because both halves call `describeContract`. What was genuinely written twice is the days-left SUBTRACTION, and that is what moved. **The accept-key refusal had no gate at all**: the test drew the screen and checked the button was absent, but never pressed the key, and proving each gate could fail is what caught it. **`controls.ts` crossed the size ceiling three times running**, because the click-path rule was written out at each binding that obeys it; it has one home now, in `ui/key-help.ts` beside the function the rule is about. And **`ordersSummary` had lost its doc comment** in 144 M4, so `orderDestinations` carried the reasoning for the summary that GitHub #27 is actually about. **Flown at Leesti**: R gives NAVY MISSIONS with no contract on it, C gives the board plus the two accepted jobs, ⇧C in flight gives the accepted half alone with a keyline reading `ESC EXIT`. The session also found two things that are correct behaviour rather than defects — a background tab has `document.hidden`, so `requestAnimationFrame` never fires and no key does anything, and the docking tunnel holds input for as long as it runs. `npm run check` passes, and every gate added was shown to fail. No save was put at risk: the page was switched to the harness namespace before anything docked.
- [x] 146 — [A click cannot press a shifted key](146-a-click-cannot-press-a-shifted-key.md) — Chris's diagnosis of 144 M6, and it was exact: *"it sounds like it's the mouse click that is not working properly? It dispatches just a letter press?"* The keyboard was never broken. `data-key` carried the key alone, `ScreenHost.click` injected a bare tap, and the plain entry answered — so `⇧I MISSIONS` clicked through to COMMANDER STATUS, and the menu cursor's Enter took the same path. His requirement, in his words: *"If a click is on a row that has a capital letter then the capital letter should be sent."* **SHIFT IS A PROPERTY OF THE TAP, not of the frame**, and that is the whole design: `commandsFor` tests every binding in one pass, so an injected tap that set a frame-wide "shift is down" would let a plain Y satisfy ⇧Y — five tonnes over the side instead of one, from a click on a menu. `Input.tapped` is a queue per code now, each tap carrying `true`/`false` when injected and `null` when it came from a real keydown, which defers to `held` exactly as it always has — so the path that was never broken is untouched by construction. `tapShift` PEEKS, because `fires` tests the modifier before it consumes. `data-shift` sits beside `data-key`, read by the click and by the cursor. 144 M6's prohibition is gone, and the gate that banned a shifted row now proves the mechanism instead. **Three of the six gates were vacuous when first written, and proving each could fail is the only reason that is known.** No shipped row is shifted — ⇧T is a keyline caption — so the row loop could only ever exercise the unshifted branch; `menuRowsHtml` takes its bindings now, and ⇧T/T is pressed through the real table. **Nothing drove `ScreenHost.click` or `runMenuCursor` at all**, and those are the two lines that were broken: `test/menu-click.test.ts` is new and holds both. And the carry test could not tell `slice(0, N)` from `slice(-N)` until its queue was longer than the limit with mixed shifts. **`controls.ts` went into ALLOWED rather than being trimmed a fourth time**: 144, 145 and 146 each added a command and each cut prose to stay under 400, which is the ceiling measuring the comments rather than the file. The entry argues the tables and the scan belong together — the three load-bearing rules are each a property of the pair — and names the split it is waiting for. **Verified against the running bundle**: a click carrying `data-shift` on KeyT opens test mode, the same row without it opens combat training, and a shifted click plus a plain Y in one frame still asks for `jettison1` and never `jettison5`.
- [x] 148 — [The tables leave the grammar](148-the-tables-leave-the-grammar.md) — Chris, 2026-08-14, reading the last three items' commits: *"What are the rules around file size. We seem to focus on trimming instead of splitting."* He was right, and `CLAUDE.md` says it outright — **never delete useful content only to fit that ceiling**. `src/game/controls.ts` had been trimmed SIX times across 144, 145 and 146 and split zero times, and every cut deleted reasoning that had just been judged worth writing. The file was two things wearing one name: the grammar (what a `Command` is, what a `Binding` is, how a table is scanned — 148 lines, unchanged in months) and the tables (210 lines, and the part that grows). `game/bindings.ts` takes `GLOBAL_BINDINGS`, `FLIGHT_BINDINGS`, `NOT_IN_THE_SIMULATOR`, `WHILE_PAUSED` and `BINDINGS`; `controls.ts` keeps the types and the scan and imports them at runtime, while `bindings.ts` imports only TYPES back, which TypeScript erases — so no runtime cycle, and `npm run portability` is unchanged. **201 and 249 lines, and the `ALLOWED` entry 146 added was DELETED rather than moved**, which was the whole point: it claimed splitting would make the code worse, and it was written by somebody who did not want to do the work. `npm run sizes` reports 31 files over the limit where it reported 32. **No prose was cut.** Both module headers were rewritten because each file now has to say what it is; every other comment moved verbatim, and a check over the **151 comment lines below the old header confirms all 151 survive**. That check is the item's own gate — a comment arriving shortened would be the refactor failing at the thing it exists to fix. `command-help.ts` was the precedent throughout: split off this same file years earlier and welded back by `Record<Command, CommandHelp>`. Three things came out of it the plan did not have, the smallest being that no NEW test was needed — a refactor that needs one has changed something, and the seven key-related test files passed untouched. It leaves `game.ts` at 2,524 lines, `ui/screens.ts` at 1,953 and `npc.ts` at 1,567, none of them examined here.
- [x] 149 — [One file holds all twenty-five screens](149-one-file-holds-all-twenty-five-screens.md) — Chris, 2026-08-14, after the exemption audit: *"go ahead and do the split"*. `src/ui/screens.ts` was **1,954 lines** behind the weakest reason in `tools/sizes.mjs`: *"one render function per screen; they share layout helpers and nothing else"* — the precondition for a clean cut, written as the argument against one, with no coupling claimed because there was none to claim. The triage found the consumers were already one per screen: fourteen files under `game/screens/` each import exactly their own render function. **Eight files now, the largest 340 lines, and the exemption is gone** — `sizes` reports 30 files over the limit where it reported 32 that morning. **The charts were four subjects, not one.** M2 deliberately deferred the second cut to measurement rather than guessing, and that was right: the moved block came to 719 lines, the gate said SPLIT, and the honest decomposition is which star is under the cursor, what is painted over the stars, and the two maps — because the two charts are two SCREENS, with two projections, two readouts and two click surfaces. **A dead re-export turned up with a false justification**: `export { distanceTenths }` claimed "every caller already reaches for it from this module", and no caller does — all eleven import it from `galaxy/navigation.ts`. Deleted rather than moved. **Three helpers had two unrelated readers each** — `portraitUrl`, `reservedNotes` and `chartKeyline` — and each got its own file rather than making one consumer a place the other reaches through, which is the item's own rule applied one level down. **Six tests named a file by path**, every one a source SCAN rather than an import, which is the class no compiler follows. Five were repointed; the sixth came out stronger, since `descriptions.test.ts` now reads the whole of `src/ui/` for a private HTML escaper instead of two named files — naming them would be a list to keep in step with a directory, the exact failure that check exists to catch. **The mechanical extraction was the hard part, not the design.** Two attempts at slicing by pattern mis-cut — once on `{ x: number; y: number }` in a parameter list, once on an arrow-function `const` ending in `;` — and both were caught by asserting brace balance on every extracted block before writing it. The third partitioned at declaration boundaries, which cannot over-run by construction. A refactor script needs its own gate. **No prose was cut**: of 349 comment lines below the original header, 340 moved verbatim; the nine that did not are two module headers rewritten on purpose and the dead re-export. It leaves `game.ts` at 2,524 and `npc.ts` at 1,568, both carrying honest `DEBT:` entries, neither touched here.
- [x] 151 — [Twenty-one comments name a caller that does not exist](151-twenty-one-comments-name-a-caller-that-does-not-exist.md) — Chris, 2026-08-14, on the comment review: *"Comments should help explain the code."* Thirty-one members of `src/` said `@internal — driven by test/playtest.js`; that file calls **eleven** of them. The claim was true once, the harness shrank away from it, and no gate joined the two files. **The split is the result, and the plan's third category is empty.** Five are reached by the orchestrator — and `law-actions.ts` and `world-build.ts` hold them because docs/TODO/150 M1 and M2 each copied a stale claim into a new file, which is why this item ran first. Ten are driven by a named test. **Six are reached by nothing outside their own class**, in two kinds: `sellCargo` and `generateContractOffers` have no caller at all, and `raiseLegal`, `openHermitTrade`, `toggleCombatComputer` and `openSystemData` are reached only by `stepHost` or by the command table, both of which are private. The plan expected a screen or a context to justify three of them; `private readonly commands` is inside the class, so it justifies nothing. **Nothing is deleted** — six members and one dead parameter are reported for docs/TODO/150 to decide. **Three more of the same defect were in plain prose**, where neither the list nor a gate sees the form: `update` named `test/gang-trial.js`, which does not exist, and `screens/trade.ts` twice named a caller that is somebody else. `tools/internal-claims.mjs` is in `npm run check` and reports **23 claims naming 28 files, 0 stale**. It reads a comment RUN whole, because a claim wraps — the line-at-a-time first draft found 22 of the 28 paths and dropped six in silence, which is this item's own failure. A call must arrive as `.name(`, or the `law-actions.ts` claim would answer itself against `game.ts`'s own declaration. All three failure modes were proved. `npm run check` passes at 4,530 assertions.
- [x] 150 — [game.ts holds five subjects that can leave it](150-game-ts-holds-five-subjects-that-can-leave-it.md) — the head of the decomposition programme, promoted from the backlog on Chris's rule of 2026-08-14: *"single responsibility does not mean put everything in one file. A file can import child files."* **Six milestones in a day, and `src/game/game.ts` went 2,528 → 1,810.** Five children, each one subject and each readable alone: the law to `game/law-actions.ts`, the sky to `game/world-build.ts`, the cockpit to `game/cockpit-view.ts`, the jump to `game/hyperspace-actions.ts` and the career to `game/career.ts`. **No milestone after M1 named its own successor, and that is the programme's sharpest lesson**: each time the plan named a favourite it was wrong, and each time the winner was an area no table had listed. M2 measured the trainer the plan HAD named and found the worst area in the file — 39 lines behind a twelve-method interface. The plan therefore writes down HOW to measure an area, and gained five warnings as it went: subtract the delegates, read the size as well as the ratio, count what an area is reached BY, a host literal travels with the thing it is handed to, and set the appliers aside before ranking. **M6 measured and stopped, which is the finding.** 75% of what remains cannot leave — an `apply*` method IS the orchestrator by this project's own decides/applies rule, the six host literals travel with the modules they serve, and the 81-line command table is deliberately the whole driving surface. Only 448 of the 1,810 lines could still move, so the file's stated target of ~300 is unreachable by taking areas out; M6 corrected that target in `tools/sizes.mjs`, because a target known to be unreachable is the same false claim in a gate's review surface that 151 was about. **M6 also got a claim wrong and corrected it in a commit of its own**: the shrink per milestone is flat at 98–138 lines, not falling — what rose is the price, 1.09 → 1.90 → 1.87 → 1.92 → 2.61 lines written per line removed. Along the way: `applyCombat`'s comment turned out to be the argument against moving it, the extraction found the game-over panel drawn from two places with one expression, and a first draft that invented two constants was reverted because a move must not create one. **Chris closed it**: *"It makes sense to split docked from flight - they are very different things."* That is docs/TODO/155.
- [x] 155 — [One file holds what a commander does docked and in flight](155-one-file-holds-what-a-commander-does-docked-and-in-flight.md) — Chris's answer to 150 M6, on 2026-08-14: *"It makes sense to split docked from flight - they are very different things. Why would you want to couple them together?"* **The code already agreed with the question**: the command table sectioned itself by mode in its own comments, `controls.ts` had a binding table per mode, and only eleven lines in 1,810 tested the mode at all. **game.ts 1,810 → 1,222 in three milestones**, into `docked.ts` (324) and `flight.ts` (395) over `flight-weapons.ts` (295) and `flight-instruments.ts` (169). **The item was judged on the boundary rather than a line count** — Chris again: *"we should not obsess over the 300 lines. What we are looking for is a clean architecture"* — so the `~300` game.ts had carried since before 150 was retired rather than re-aimed. **The gate caught a design fault**: M2's first draft was one file of 648 lines with five section headers, and `sizes.mjs` failed it; an exemption was available and would have been dishonest, because the ALLOWED list's own rule is that a reason must say why a file cannot be a parent plus children. **M1 found the docked menu drawn from three places** with the same three-part expression, invisible while both modes shared one file. **M2 found the gun and the exercise are a construction cycle** — an exercise fires the career's own `Combat` and credits its own kills — resolved with a thunk, because a second `Combat` would make a shot resolve differently in a trainer. **M3 gave the hermit to the docked half**, and the argument was one home rather than the mode machine: `tradeContext` already held `leaveHermit`, so the file's sentence widened to *what a commander does when the ship has STOPPED*. **Neither half reaches into the other**: five of flight's seventeen host methods are ways OUT of flight, and the parent decides what the game becomes. The parent's own sentence is the test the item closed on: **the orchestrator — which mode the game is in, and who gets the frame.** One mistake worth keeping: eleven doc comments were dropped by writing fresh prose for moved members, and comparing comment lines before and after is what caught it. `npm run check` passed at 4,530 assertions, unchanged, through all three milestones.
- [x] 152 — [The map at the top of game.ts is out of date](152-the-map-at-the-top-of-game-ts-is-out-of-date.md) — the header's three wrong claims: eight modes where the type had fourteen, a HUD fed from a file that no longer feeds it, and Vipers this file no longer launches. **155 M3 removed all three as a side effect** of stating the parent's one responsibility, and the mode list is GONE rather than replaced — which is why **M2 correctly produced no gate**, the outcome the plan itself named as better. **The audit then found two more, and one was mine**: the header I had written an hour earlier said `stepHost()` lives here, and 155 M2 had just moved it to `flight.ts`. **The defect this item is about, reintroduced by the commit that fixed it** — which is the argument for the rule that came out of it, now in `CLAUDE.md` beside the module-header line: *the milestone that takes a responsibility out of a file repairs that file's header in the same commit.* Eleven other claims were checked mechanically and all held: `baseMode` is assigned here and in neither half, the two halves import each other zero times, the three appliers are in the halves, and the console's four writers have one home each. A third stale claim turned up in the other review surface — `tools/sizes.mjs` still said 155 "is splitting" the file — and that entry is rewritten around the question the list actually asks, is **no longer a DEBT** because the file can state one responsibility, and names the six screen openers as what could still leave so it does not become a place to hide.
- [x] 153 — [A rule explained where the rule does not live](153-a-rule-explained-where-the-rule-does-not-live.md) — Chris, 2026-08-14: *"Comments should help explain the code - but they are only needed if the code is not self explanatory."* **The premise inverted under measurement, and that is the item's result.** Walking every rule paragraph in `game.ts` and its nine children with the plan's own test — delete the code in your head, is the sentence still true? — gave **0 homeless paragraphs and 9 duplicates**. Every rule beside a handler was a second copy of prose the rules module already carried, usually in better words: `jettison.ts` opens with the pirate rule, `contract-offers.ts` says "deliberately more generous than the original" on the function itself, `prewarm`'s doc owns the warming rule, `characterVerdict`'s owns "only a crossing speaks". Two were word for word — `massLocked`'s summary is `world-step.ts`'s sentence exactly, `placeOf`'s is `audio.ts`'s — and one was mine, from 150 M4, where `hyperspace-actions.ts`'s header quoted `hyperspace.ts`'s opening back at it. **So the defect is not homelessness but one rule with two homes**, which is the failure the codebase is organised against wearing a different face. **The plan's central example was wrong on the facts**: it said a reader wanting the pirate rule would open `jettison.ts` and not find it there; they will. **The test needed a second half** — then open the module that owns the rule and look — and that half is mechanical, so it was measured: a word-bag comparison of every comment sentence against every other in `src/` found 27 echoes in the family, and one reading pass sorted nine true duplicates from the false positives. **The false positives are the interesting half**: most are a SEAM stated from both sides, each true only from its own, so both stay. **M2 asked whether the tree-wide sweep was worth it and the answer is no**: 175 pairs across `src/`, of which 32 are a constant beside the module that spends it — which `CLAUDE.md` explicitly requires — and 69 are generated headers. `game.ts` was the outlier the item suspected, and structurally so: an orchestrator's handlers sit beside the rules they spend. The detector is not made a gate, because it would fail the build on prose the rules demand. Nothing was cut: the ten files went from 1,653 comment lines to 1,655.
- [x] 156 — [The escape capsule nobody chose to shoot](156-the-escape-capsule-nobody-chose-to-shoot.md) — GitHub #28, from Chris's flight: *"Escape pods seem to be too easy to destroy. And destroying a pirate's escape pod should not make me a criminal."* **Two faults with different causes, and one data change both need.** A capsule launched AT the wreck, which is the one place the gun is certainly already pointed, and a beam laser fires ten times a second — so the burst that killed the ship killed the pilot, and the commander never chose it. `POD_LAUNCH_GRACE` is 1.5 seconds of it, and `shot.ts` SPENDS it in both passes, so a graced capsule can be neither struck squarely nor grazed. The number is not a taste: a capsule drifts at 40 to 70 units a second and leaves the graze cone once it is 16 off the line of fire, which is 0.4 seconds in the worst lateral case; the rest is the held trigger. **The other half is who was inside.** Every capsule was a Fugitive offence whoever was in it, so shooting a raider's pod outranked shooting the raider — and the capsule could not answer, because `Combat.wreck` despawns the ship in the frame that launches it. It carries `occupant` now and the record comes off `offenceFor`, the same rule that prices the hull. **Triage found a third thing the report did not**: the Character ladder was never charged for this at all, so a lawful pilot could be shot in his capsule and the fine bought back a spotless name. It is `DISREPUTE_MURDER` now, and a raider's capsule costs it too — Chris's call on 2026-08-15, *"Character only"*: not a crime, and still a deed. `SNAPSHOT_VERSION` went to 2 rather than take a default, because guessing an occupant decides a commander's record for them. combat.ts crossed 400 on the way, and the seam its own header had named for years became `combat-player.ts`, which took `GameState` out of the pure rule. `test/escape-pod.test.ts` is the gate; six assertions go red when the two fixes are reverted, and `npm run aim-probe` is byte-identical either side.
- [x] 157 — [The console line runs off both edges](157-the-console-line-runs-off-both-edges.md) — GitHub #29: *"Constrictor mission - popup text didn't show all lines"*, and Chris named the surface as the amber console line. `#message` declared `white-space: nowrap` and **no width at all**, centred on `left: 50%`, so a line wider than the window hung off BOTH edges and you read the middle of the sentence. **The measurement is the whole item**: the Constrictor gun warning is 91 characters, which at 15px Menlo with 3px of tracking is 1095 pixels against the 942 that one row of an ordinary 1024px window holds. It has never fitted. The two numbers that make the sentence useful — what your gun scores and what the best gun scores — sit at the two ends, which are the ends that clip. The queue was never at fault: `later` and `tickMessage` deliver the line whole and it is then painted off the screen. **The words are untouched, and that is the point** — docs/TODO/144 M1 already cut that sentence once on a length argument and then put it back, and writing a rule to a width is what this stops. The gate lives beside the crosshair's decided CSS twin in `test/ui.test.ts`, and it measures the line FIRST so that it is not the stylesheet asserting itself.
- [x] 158 — [The safe zone that only the spawner obeys](158-the-safe-zone-that-only-the-spawner-obeys.md) — GitHub #30: *"I was attacked by a bounty hunter when I was in range of a space station. I think pirates and bounty hunters should not engage when you are in range of a space station."* **The behaviour was correct under the rules that ran**, so the fault was in the rules. A safe zone was already written down in `AMBUSH_STANDOFF`'s own doc comment — *"the one place where a player can catch their breath"* — and exactly one rule read it: the spawner, which refuses to warp a pirate wave in near the station. `isHostileToPlayer` took a ship and a legal status, so it could not answer a question about the station at all. **The triage found the cause neither report named: the amble.** Every idle ship drew a waypoint 800 to 3,300 units from the STATION, for every role except the trader, so a system's hostiles converged on the port whatever they were spawned at — which is also why deep space is empty (docs/TODO/159). The constant is `STATION_TRUCE` in `constants/law.ts` now, with two readers, and `truceHolds` is the rule. It is asked inside `isHostileToPlayer`, so the ship, the scanner blip, the threat arrow, the condition light, the bought combat computer and the bribe key give ONE answer. **The police are deliberately not covered**: a station that hid a Fugitive from the law would be the one place a record stopped costing anything. **A commander who shoots first ends the truce**, on `provokedByPlayer`, so the port is not a free firing position. `test/station-truce.test.ts` is the gate and both halves were shown to fail separately — 13 assertions for the rule, 2 for the waypoint — and `defence-probe`, `survivability` and `dock-traffic` are byte-identical.
- [x] 159 — [The lane that only exists at the station](159-the-lane-that-only-exists-at-the-station.md) — GitHub #31: *"Flying from the space station to the sun — I don't think I encountered any NPC ships. We should come across some people."* **Measured, the report was exact.** The sun sits 320,000 units from the system origin and the station about 12,000, so the run to the fuel-scoop band is roughly 220,000 units — about 70 seconds under the torus drive. Everything a system holds is within 22,000 of the station: the widest thing placed is the rock hermit, and a trader that warps in later lands at `TRADER_ARRIVAL_RANGE`. The ONE spawn anchored to the commander is a pirate wave that `LAWLESS_GOVERNMENT` refuses for half the ladder, so in a system of government 4 or higher nothing was ever placed near the commander at all. **The fix moves an anchor and nothing else.** `stepEncounters` keeps its clock and its cap and says WHERE the trader it already ordered goes; `spawnPassingTrader` is the placement. It is `departing` rather than `arriving`, because a ship pointed at the station out there would fly for sixteen minutes and hold one of the four trader slots for all of them. **The cone is DERIVED from `MASS_LOCK_SHIP`** — the widest angle at which the arrival still mass-locks a commander who holds course — so the meeting is a meeting rather than a dot. The first derivation used `SCANNER_RANGE` and was measured and rejected: that guarantee is static and a departing trader is not, and about one run in ten met nobody. **`spawning.ts` crossed 400 lines on the way**, and the seam was already in its own header: the arena is `spawning-arena.ts` now. Measured, 20 sun runs of 20 meet somebody and 60 of 60 do, with exactly one ship inside scanner range in every flight; the same flight before the change met nobody over 200 seeds.
- [x] 160 — [A record you cannot work off](160-a-record-you-cannot-work-off.md) — GitHub #32: *"Killing pirates should decrease your criminal status."* **A legal record only ever went up.** `raiseLegal` raised it, and `recordCleared` — the fine paid at a station, by choice — was the only rule that took it down, and its own doc comment said so in as many words. `recordWorkedOff` is the second way. **`KILLS_PER_RUNG` is five, on arithmetic rather than taste**: a pirate's bounty runs 4 to 22 credits across the roster and most of the band sits near 10, so five kills earn about 50 credits against a rung that costs 25 as an Offender and 75 as a Fugitive. The fine is the fast way and needs a station; the fight is the slow way and needs none. Ten kills take a Fugitive to Clean. **The record moves and the NAME does not** — disrepute is untouched, or a commander could murder a trader, shoot five pirates, and end Clean and Honest at a profit. That is docs/TODO/156's split read from the other side, and it is what makes the feature safe. **Pirates only**, because a mothership replaces a Thargon every five seconds and counting drones would make a record free. A Clean commander banks nothing, so a crime cannot be paid for in advance, and a fresh offence clears a part-paid ledger. The atonement is a `CombatEvent` (invariant 15) and goes straight to `LawActions` rather than out through the host — raising a record launches the Vipers and is the orchestrator's act, while a record worked off queues one console line. `commander.atonement` is saved state, so `SNAPSHOT_VERSION` went to 3, and seven unrelated constants gained `@rule` ids because the value 5 is shared nine ways. `test/atonement.test.ts` is the gate; 13 assertions go red without the rule and 1 without the reset, and `npm run elite-a` is byte-identical.
- [x] 161 — [The game refuses an old save rather than raising it](161-the-game-refuses-an-old-save-rather-than-raising-it.md) — Chris, 2026-08-16: *"We should migrate snapshot v2 to v3."* `SNAPSHOT_VERSION` moved twice on 2026-08-15, and `parseSnapshot` refused anything that was not the current number. **The refusal was silent in the worst way**: `SAVE_RECORD_VERSION` did not move, so a version 2 record is on the shelf and IS listed — the player picks it and it throws. `MIGRATIONS` sits under `SNAPSHOT_VERSION`, because that constant's doc says what each version ADDED and this says how to add it. `migrateSnapshot` climbs on a COPY and only when it has a step to run, so a current snapshot is not cloned and one it cannot raise leaves the caller's bytes untouched; a version it does not know falls through to the check that already refuses it. **The v2 step WRITES `commander.atonement`** rather than permitting its absence, because `Persistence.restore` clones the commander straight in and an absent field reaches `recordWorkedOff` as undefined — a ledger at NaN. (**Corrected 2026-08-16 by docs/TODO/167**, which measured it: `NaN < KILLS_PER_RUNG` is false, so the first pirate kill takes a whole rung and the ledger then heals itself. It is a gift of four kills rather than the silent loss this entry first claimed, and the migration is still right.) **Two things the work found.** `parseSnapshot` returned `raw` rather than the object it had validated, which was the same object until a migration copied it. And the first gate DIED rather than failing — proving it could fail found a bare call ending the run, so a gate that cannot report its own failure is not a gate. `snapshot.ts` crossed 400 lines and the seam was already a banner in the file: the door is `snapshot-parse.ts`, 157 lines against 311. Version 1 is still refused, and the plan holds the argument both ways.
- [x] 162 — [One word that means five things](162-one-word-that-means-five-things.md) — GitHub #33, and Chris read one line: *"'Cost you name' doesn't mean anything. We use it in a lot of places and 'name' is a bit confusing - what are we saying."* **The report was exact and the cause was wider.** `name` carried five meanings: what a thing is CALLED, the disrepute ladder, the legal record, the combat rating, and a value's own label. The first is the one the game TEACHES — three screens ask for a name and each means the word you type — so `COST YOUR NAME` read against that lesson. The worst case was one paragraph in `law.ts` holding two of the meanings four lines apart, in a paragraph whose point was that the two are separate; the same sentence also carried a claim docs/TODO/160 had made false. **The item was re-cut mid-flight, and that is the record's main point.** M1 landed saying `CHARACTER`, which swapped one internal word for another. Chris: *"a user does not have the context we have and they don't understand all our internal ways of naming things"*, then *"2-3 lines maximum on the console. More text as needed in the main UI"*, then *"this does not mean we write essays - just think - if this was the first time I saw this string - would I know what it meant."* So the player's word is **REPUTATION**, the scope became every consequence line, and each line says what happened rather than naming it: `REPUTATION: DODGY — WORD IS GETTING ROUND`, `LEGAL STATUS: FUGITIVE — POLICE AND BOUNTY HUNTERS WILL ATTACK YOU`. **The verdict says which WAY it moved**, because a rung name has no sign on it and the decay crosses rungs downward; the clause is about talk rather than a rule, because what a rung COSTS differs by rung. Five identifiers followed the prose, and `characterName` was the sharpest case the plan never named — `COMMANDER ${c.name}` and `characterName(c.disrepute)` sat six lines apart on one screen. `test/ladder-words.test.ts` is 17 assertions in three parts, and all four protected rules were proved able to fail. `NAME` is deliberately not banned: it is the one correct meaning. 4,708 assertions, 387 exports and 76 rule ids unchanged.
- [x] 154 — [The comments in src/ are not in Simplified Technical English](154-the-comments-in-src-are-not-in-simplified-technical-english.md) — Chris, 2026-08-14: *"Are they in ASD-STE100?"* They were not. `CLAUDE.md` set the style and stated that no gate checked it, and a rule with no measurement is a preference. **The measurement is the whole item.** `tools/ste.mjs` counts three rules a machine can count — the sentence caps, the `-ing` words and the tense — and found 1,988 long sentences at 20% of every sentence in `src/`. It also found the proof that the incremental rule fails: `src/constants/`, swept in one pass by 141, read at 7%, and the rest of the tree, left to convert as each file was edited, read at 24%. **The harder half is `tools/ste-read.mjs`, which decides what is measured**: the style never touches code, an exact command, an API name, an error string, or anything quoted from a person, and a quotation rewritten is falsified. It walks characters rather than lines, because a line-at-a-time reader cannot tell a comment from a string that holds `//`. **M3 was the sweep, at twenty-nine passes over about 130 files**: 1,988 to 0 long sentences of 14,582, and 309 to 0 compound tenses. **The tail reads worse than the head, by share** — a file low in `--work` order starts at about 30% over cap, exactly like a file at the top, and merely clears in fewer edits. **A last breach is nearly always a header that states three or four things in one sentence**, and the remedy is the style's own vertical list. **A converted file drifted back while the sweep ran**, which is the evidence the gate rests on: `game/npc.ts` reached 0% on 2026-08-14, docs/TODO/158 put five long sentences into it on 2026-08-15, and nothing said so. **One real defect came out of the sweep**: `ships/elite-a-hulls.ts` claimed the pack's gun-vertex byte is 0 for thirty of the designs and that only five ships name a later vertex, then listed six names; it is thirty-two of 38, and six ships. **M4 is the gate — `npm run ste:check`, whole-tree, inside `npm run check`.** The plan expected diff-scoped; whole-tree costs the same on a tree at zero, lets less through, and needs no diff base. It holds two rules of the three, and the `-ing` count never gates. Proved able to fail by a 30-word sentence and by a compound tense, both removed, and lastingly by four assertions that run the real command line against a fixture. 4,708 assertions, 387 exports and 76 rule ids unchanged.

- [x] 164 — [The tool that reports every file as untested](164-the-tool-that-reports-every-file-as-untested.md) — found by the sweep of 2026-08-16, and it is one wrong path in a tool nobody asserts. `tools/coverage.mjs` split every script URL on `/elite-web/`, and the checkout is `harmless`. So the split missed, every path became `undefined`, and the report named **259 of 260 files as never executed**. The list held `constants/law.ts` and `world-step.ts`, which the suite drives thousands of times. **The tool's own header says which half matters**: *"The number that matters is not the percentage, it is the LIST at the bottom: files no test touches at all."* That list was 100% false, and a tool that names every file as untested is worse than no tool. **The fix asks the process rather than a name.** `process.cwd()` is the root, and a script from outside the checkout is dropped rather than collected under one `undefined` key. The real picture is **247 of 259 files touched and 12 never executed**, and two of the twelve export types only. **The self-check is the gate**: the tool exits 1 when the touched count is under half of the files found. That covers the wrong root and a run from a subdirectory, which is the other way `cwd` can be wrong. Proved able to fail by putting `/elite-web/` back. **Nothing was renamed**, because `elite-web-` is the live save namespace and a rename orphans every save on every shelf. **The second half shares one root with the first.** `tmp-jump.ts` sat in the repository root and git tracked it, from commit 46828fb on 2026-08-11. `tsconfig.json` includes `src`, `train`, `test` and `tools`, so `npm run lint` never read it, and it had stopped compiling: `Expected 5 arguments, but got 6`. It was added to the include list for one step to prove the hole, then deleted. `tmp-*.ts` is in `.gitignore` now. **The tool stays outside `npm run check`**, because it runs the whole suite a second time and the self-check answers the same worry for nothing. 4,708 assertions unchanged.

- [x] 163 — [The chart key that needs a browser](163-the-chart-key-that-needs-a-browser.md) — found by the sweep of 2026-08-16, and pointed at by docs/TODO/164's repaired tool. `src/engine/shell.ts` promises a headless game and `ui/screen-shell.ts` holds the seam that keeps it, but `screens/chart.ts` reached for `document.getElementById` directly. **The two painters beside it read the SAME two element ids through the seam**, so the screen and its painters disagreed about one lookup. The line runs under a guard that the `F` key turns on, so type-to-find threw under node: `ReferenceError document is not defined`. **The browser was never affected and no player could see this.** The cost was the seam — no headless test, replay or harness could drive that path at all, and the corrected coverage tool read the file at 89.3%, the worst screen in the tree. The fix is one line: `maybeById`, which answers `null`, and the `if (info)` guard below it already handled null. **The gate is a new file rather than a block in `test/chart-days.test.ts`, and that is a deviation from the plan**: `chart-days` paints through `test/screen-capture.ts`, which INSTALLS a recording document, and this gate needs none at all. A no-document block inside a file that installs one could pass for the wrong reason. `test/chart-headless.test.ts` is 11 assertions and drives BOTH charts, because the line branches on `this.local` and reads a different id on each side. Its first assertion is the control: there is no document. **The cursor check is what proves the path is reachable** — `L` moves the cursor to Leleer and `A` narrows it to Lave, in two calls rather than one, because `typeToFind` drains a whole frame at once. Proved able to fail: six of the twelve checks go red with the old line back. **`screens/chart.ts` went 89.3% to 91.8%**, measured with 164's tool. **Three other direct callers of `document` stay** — two anchors and a file input, in `save-transfer.ts` and `combat-sim.ts`. Each is a platform ACTION rather than a paint, and a file download has no headless meaning. Do not re-open them. **One thing the plan did not have**: `maybeById`'s doc said *"These four callers"* and this made a fifth, so the count is gone. 4,719 assertions, 387 exports and 76 rule ids.

- [x] 167 — [The ledger that pays a rung for one kill](167-the-ledger-that-pays-a-rung-for-one-kill.md) — found by the sweep of 2026-08-16. The comment that justifies docs/TODO/161's version 2 migration stated the opposite of what `recordWorkedOff` does. It said an absent `atonement` leaves a commander who *"can never work a record off again, and nothing says so"*. **Measured, `recordWorkedOff(FUGITIVE, undefined)` returns `{ legalStatus: 1, atonement: 0 }`** — `undefined + 1` is `NaN`, `NaN < KILLS_PER_RUNG` is false, so the rule skips the "part paid" branch and takes a whole rung on the FIRST kill. It then writes 0, so the ledger heals itself, and `LawActions.lowerLegal` queues `recordVerdict` to announce the rung. **The comment named a silent loss; the behaviour is a loud gift of four kills.** A third claim was false too: `git log -S` shows `atonement` and `SNAPSHOT_VERSION = 3` arrived in ONE commit, so no version 2 save can hold a part-paid rung and the raise costs nothing. **THE ITEM'S REAL FINDING IS THE COUNT: one wrong sentence had six homes**, and the plan named two — the migration comment, `docs/TODO/README.md`, 161's own plan doc, this index, `test/snapshot-migrate.test.ts` and `test/atonement.test.ts`. **A record is corrected rather than rewritten**: the three archive sites keep their words and carry a dated correction note, because the house style does not rewrite what somebody decided, and a false FACT is a different case. **M2's answer is no guard.** The rule stays three lines and trusts its two numbers; the cause is a save with no field, and two layers already answer it. The two new assertions went into `test/atonement.test.ts`'s rule block rather than the migration file, because that is the home of every other `recordWorkedOff` assertion, and the plan's third assertion was dropped as a duplicate of the check four lines above it. **Both gates were proved able to fail separately** — the version 2 entry out of `MIGRATIONS`, and the rejected guard put into the rule, which reddens exactly the two new assertions and nothing else. That second proof is also M2's evidence. **The shelf layer is read from the code and NOT measured**: `repairCommander` is private and `readSave` needs a store node has not got. 4,721 assertions, 387 exports and 76 rule ids.

- [x] 165 — [A citation that names nothing](165-a-citation-that-names-nothing.md) — found by the sweep of 2026-08-16. The tree cites a plan document more often than it cites an invariant, and **three of those numbers resolved to nothing**. One was cited from `src/`: `break-off.ts` said *"TWO WORDS SINCE docs/TODO/68"*, and a reader who wanted to know why the readout says two words had nowhere to go. **The index and the archive agreed with each other**, which is why nobody saw it: a number that reached neither is invisible to both. `tools/plan-claims.mjs` is the gate, in `npm run check`, at **1,177 citations naming 106 plans of 151, 0 unresolved**. It matches the NUMBER rather than the slug, because a plan is renamed by its own milestones. **The gate found more than the plan did, and that is the point of writing it first**: an extra check on citations that carry a whole file name caught `90-the-survey-counts-every-constant-outside-the-constants-directory.md` under `docs/TODO/`, whose number resolves and whose path does not — that document moved into `completed/` and the citation beside it did not follow. **That check cannot tell a citation from a sentence about where a file USED to be**, which this plan's own paragraph about 68 was. **The gate also failed on its own plan document**: Verification step 1 said to cite a number no file carries, and the gate reads `docs/`, so that sentence WAS a broken citation. **68 landed and went to `completed/`.** **81 carried TWO plans and the plan above did not know it** — one is superseded and is in `retired/`, and the other was never actioned and is [81](81-live-picker-cannot-name-attack-run.md), fixed inside this item on Chris's call. **147 is allowlisted in the gate**, also Chris's call, so nothing is reconstructed from memory and 162's citations stay as written; **the allowlist guards itself in both directions**, because a number that gains a document makes the exception a lie and a number nothing cites makes it hide nothing. Proved able to fail four ways, and the third is the one that matters: **a citation split across two lines of one comment is still found**, which is the failure docs/TODO/151 records. 4,715 assertions.
- [x] 166 — [The map still claims that game.ts owns the entities](166-the-map-still-claims-that-game-ts-owns-the-entities.md) — found by the sweep of 2026-08-16. The decomposition programme moved nine responsibilities out of `game.ts` and four more splits followed it. **Every module header was repaired. `docs/ARCHITECTURE.md` was not.** docs/TODO/152 wrote that rule for a FILE, and the map is not a file's header, so the rule never reached it. **The plan named three false claims. The repair found five.** The two extra are the same defect at another site: `missionBlueprintOverride`'s only caller is `world-build.ts` rather than `game.ts`, and the chart painters left `ui/screens.ts` in docs/TODO/149. **A sixth line was stale prose** — the console bullet still said *"what a deed cost your name"*, which docs/TODO/162 retired. **The fourteen modules are named now**, in a section shaped like `game.ts`'s own header: the two halves over their children, then the seven subjects. The roster block lost four sentences to the three headers that already state them. **`CLAUDE.md`'s rule covers the map** in one clause, and `docs/PROCESS.md` does not restate it. **`npm run map:report` is the check, and it never fails a build**: a gate that demanded a line per file would turn the map into an index. It went **42 of 56 unnamed to 28 of 56**, and it is proved able to detect — one name taken out of the map moves the count by one. 4,715 assertions.

- [x] 172 — [An empty console line still draws its box](172-an-empty-console-line-still-draws-its-box.md) — GitHub #37. Chris flew it and photographed it: *"There's a left over rectangle when there's no console text."* `body.screen-open #message` gives the console line a background and a border, because a screen is 92% opaque and amber text over one needs a ground of its own. **Nothing asked whether the line holds any words.** `#message` is `position: absolute`, so it is a block box, and an empty one still paints 12px of vertical padding and a 1px border. **The open question is answered, and the code was the authority.** The report said the rectangle only appears after some text has been shown. Measured in Chrome at the first frame of a docked career, the empty element painted **34 by 14 pixels** — 16 + 16 + 1 + 1 across, and 6 + 6 + 1 + 1 down, which is its padding and its border and nothing else. So the box is there before any message, there is one cause, and a pilot notices it after a line fades because that is the moment the words leave. **The fix is `#message:empty { display: none; }`**, beside the block that gives the plate, and `hud.ts` is untouched: the painter owns one fact already — whether the timer still runs — and a class it also toggled would give one rule two homes. Measured after the change: an empty line reads `display: none` and 0 by 0, and a line put back reads `display: block`, 527 by 37, with both the plate and the border. **`test/console-plate.test.ts` is 4 assertions and holds three claims rather than two.** The stylesheet pair is the first two. **The third is behaviour the stylesheet cannot state**: `:empty` needs an element with no child node at all, so the painter must leave none, and the test drives the real `Hud` through `test/screen-capture.ts` to read the element back. **THE PAIR IS ASSERTED DIRECTLY, AND THAT DEVIATES FROM THE PLAN**, which asked for a gate that goes green when the plate is deleted as well. The plate is a decision the plan records, and a decision with no measurement is a preference; each failure message names the other half, so a reader who drops the plate on purpose is told the `:empty` rule is then unnecessary. **Proved able to fail three ways, each alone**: the `:empty` rule deleted; the plate deleted as well; and `hud.ts` writing `' '` in place of `''`, which reports `got " ", want ""`. **The third is the risk the plan expected to escape the gate.** `#prompts` was not touched, and the measurement supports the plan: no background, and a border width of 0. 4,719 assertions.
- [x] 170 — [The rocks are all at the station](170-the-rocks-are-all-at-the-station.md) — GitHub #34. Chris flew it and reported: *"There always seem to be some asteroids near the space station. I think they should be spread along the path."* **The report was exact, and the placement was by construction.** The rock loop anchored every rock to the station, on an arrival and on a launch alike, so the run in from the witchpoint held no scenery at all. The file already held the answer: `corridorPos` sat ten lines above the loop, and the traders, the police and the pirates all used it. **The rocks are the one thing that never asked.** They read the situation now, in the two-branch shape the police already had: an arrival strings them down the lane, and a launch keeps the station anchor, because a commander who starts at the slot has no lane to string a field along. The count and the seed expression are untouched, so a system shows the same rocks on every visit. **`ASTEROID_LANE_SCATTER` is DERIVED rather than chosen, and the plan did not have that.** `scatter()` places a rock at up to 1.5 times the nominal, so `SCANNER_RANGE / 1.5` puts the outermost rock at exactly scanner range, and the whole field is on the scanner as the commander passes it. The measured furthest rock is 5,961 against a range of 6,000. `DEEP_TRADER_CONE` measured the same derivation and rejected it, because a trader moves off the scanner; a rock does not move. **The derivation is also what answers `constants:check`.** The plan expected an `@rule` id; written as the literal 4,000 the constant repeats five others, and the checker's own message offers a derivation as the other remedy. **Measured over 40 systems at the real witchpoint distance, of 117 rocks: an arrival went from 64 inside `MASS_LOCK_STATION` to 0**, with the mean nearest rock 3,652 → 27,477 units from the station, and a launch unchanged at 57. The rock count is 117 on both sides. **`test/spawning.test.ts` sweeps both situations now**, because one arrival sweep could not read a launch band at all, and neither branch is evidence for the other. Six assertions: the lane band, the launch band, the mass lock on each side, the scanner derivation, and the police launch spread, which had no measurement before either. **Proved able to fail twice and separately** — the station anchor put back for an arrival reddens the three arrival claims alone, and the corridor used on a launch reddens the launch claim alone. **Both probes are byte-identical, and neither one measured the change**: `roster-probe` reads `roster-census.ts` and `dock-traffic` builds its own approach, so neither calls `spawnPopulation`, whose only caller in the tree is `world-build.ts`. They prove that nothing else moved. `dock-traffic` reports 80 of 80 docked and 0 rams. **`HERMIT_SCATTER`'s doc comment is reported and not fixed**: it claims 2.5x the asteroid field's nominal radius, and 14,000 against 5,000 is 2.8x. 4,753 assertions.

- [x] 171 — [The briefing says reputation when it means rating](171-the-briefing-says-reputation-when-it-means-rating.md) — the triage of GitHub #36, which reported a string docs/TODO/162 had already fixed: a commit permalink pins a tree, so a search result that carries one never moves. **The triage then measured the tree.** docs/TODO/162's gate read a shouted string, in TypeScript, under two directories — about a third of what a player reads — and it stripped every comment before it read. **There are three rules over three surfaces now, and each one fails on its own**: the shouted rule, unchanged; every mixed-case sentence a player reads, from the seven pages of the site and from the screens and the briefing; and every comment in `src/`. **The three lists stay apart**, because one list that served all three would ban a correct row of each. **The prose rule is two tests and neither bans the word**: `reputation` is never negated, because Honest is the BEST rung of that ladder and a fresh commander therefore has one at its best, and it is never plural, because a plural is a fourth meaning. The novella's *"Commanders of established reputation"* passes and is correct. **The comment rule reads the ladder word beside `name`, and a paragraph is the unit**, because the word and the ladder are often two sentences apart; `name` is wrong only where the comment never says WHAT is named, and the failure message asks for exactly that. `constants/character.ts` and `game/character.ts` are exempt, because they state the rule. **THE GATE FOUND TWENTY SITES AND THE PLAN NAMED SIX.** Seven of the extra fourteen are the same defect elsewhere, including the second PUBLISHED one at `constants/threat.ts:81`. **`game/commander.ts` is the mirror fault and no rule in the plan covered it**: it called `combatScore` a *"Combat reputation"*, which is the other ladder's word since docs/TODO/162, so the comment rule has a second half. **Six sites were correct and are still repaired** — each said `name` without saying what is named, so `the name rules` is `the save name rules` and `the name on the status screen` is `the rung name on the status screen`; a file exemption was the other remedy and would have blinded the gate to a whole file. `CATALOG.md` lost both of its `name` rows. **Chris chose all three sentences on the pages** (2026-08-16): the briefing says *"a rating of Harmless"*, and the manual says *"no rating whatsoever"* and *"danger builds along lawless routes"*. **`tools/ste-read.d.mts` is what the plan did not have**: `tools/` is plain JavaScript and `tsconfig.json` sets no `allowJs`, so the comment axis could not import the reader at all, and the declaration file is why it reuses `tools/ste-read.mjs` rather than writing a second character walker. **The milestones landed in the order M3, M2, M1, and that is a deviation**: M1 was written first and found the full list before any commit, and the COMMIT order is chosen so that every commit passes `npm run check`, because a gate cannot land red. **Each rule reports what it read** — 815 shouted strings, 703 player sentences over 7 pages, 7,179 comment paragraphs. **Proved able to fail six ways, each alone**, and the page walk pointed at a directory with no pages leaves the rule green and reddens its control, which is why the control exists. **One thing is reported and not fixed**: `novella.html:313` says *"attempts on your name"* and means the disrepute ladder, and no rule reads `name` on a page. 4,739 assertions.

- [x] 168 — [The style checker never reads the documents](168-the-style-checker-never-reads-the-documents.md) — found by the sweep of 2026-08-16. `npm run ste:check` read every comment in `src/` and no document at all, because a markdown file holds no comment. The house style also covers ten markdown documents and each TODO item, and docs/TODO/141 swept those ten in one pass on 2026-08-12 with nothing to hold them since. **The reader came before the number, and the plan was right to insist**: its own trial reader joined a whole list into one 43-word breach, so the figure it produced meant nothing and is not in this record. `tools/ste-read-md.mjs` decides what a heading, a code block, a table and a link are, then hands the paragraph to the three functions `tools/ste-read.mjs` already exports, so a sentence is split one way for both readers. **A list item is its own sentence**, which is the rule the trial reader got wrong. **The real measurement is 174 sentences over the cap and 35 in a compound tense, of 2,559; it is 0 and 0 now, of 2,905.** The ten documents held 48 of those breaches and the index held 150, of which 144 were in the dated sections. **CHRIS ANSWERED TWO SCOPE QUESTIONS, AND THE MEASUREMENT IS WHAT ASKED THEM.** A section heading keeps the caps and loses the title rules, because 73 of the 88 headings in the ten documents carry no verb and `docs/PROCESS.md` mandates a plan shape whose own section names are noun phrases; `CLAUDE.md` says "an index label" where it said "a heading", with the count beside it. And the gate reads the whole index rather than a split scope, so 144 record sentences were rewritten. **`tools/titles.mjs` landed before this item ran**, and it is what the plan's title count and title rules asked for — it reads 315 plan titles and index labels — so M3 added no second home for that rule. **A BLOCK QUOTATION IS READ, AND M1 SAID TO DROP IT**: the plan's reason was that this repository quotes a person in a block, and measured that is false — `docs/PROCESS.md` holds no block quotation at all and its four quotations of Chris are inline, so the 95 block lines are house prose and a dropped block would take all of them out of the gate's reach. **Two reader defects came out of the measurement**, both in the half `tools/ste-read.mjs` already owned: a hash opens a sentence, so one paragraph of the index read as one sentence of 112 words rather than sixteen; and a quotation of a whole sentence carries its full stop INSIDE the quotation marks, so the mask took the terminator away and joined the sentence after it. **One false claim came out of the sweep**, which is docs/TODO/154's finding again — `docs/COMBAT-SIM.md` said the machinery for a live career selection exists in `brain-names.ts` and named `liveBrainSelection` and `liveBrainId`, which docs/TODO/81 deleted the same day. **Proved able to fail five ways, each alone**: a 30-word instruction in `docs/PROCESS.md`; a compound tense in `docs/INVARIANTS.md`; the same long sentence inside a fenced code block, which stays GREEN; the same sentence in a table row, which stays green; and a document named in `DOCUMENTS` that is not there, which reports the name and exits 1. **`README.md` stays out**, and the plan's open question about where its first-person opening ends is unchanged. The gate reads 14,620 sentences in 252 files and 2,905 in 13 documents. 4,739 assertions.

- [x] 169 — [npc.ts holds behaviour and brain flight in one file](169-npc-ts-holds-behaviour-and-brain-flight-in-one-file.md) — promoted from the backlog by the sweep of 2026-08-16, and the head of the decomposition programme. `src/game/npc.ts` was 1,632 lines over 99 commits, and it was the one file of 259 in `src/` with no module header at all. It is 1,536 lines now, and two files came out of it. **THE DEBT ROW WAS WRONG ABOUT THE SPLIT, AND SO WAS THE PLAN.** `tools/sizes.mjs` named the flight half for months. M1 wrote the header first and landed it alone, on the backlog's own argument that stating the one responsibility is what exposes the second. M2 measured and the cheapest cut was the fleet queries, which the row never named: `isHostileToPlayer`, `hostilesNear`, `nearestEngaging` and `nearestNpc` are `game/hostility.ts` now, at 169 lines. **They take a narrow interface rather than the class**, so the rule six surfaces read (docs/TODO/158) names no ship class, and `test/hostility.test.ts` holds that with a source scan, a control that proves the scan can see one, and a fixture of one object literal — because a scan cannot say whether a type is honestly narrow. A generic type parameter is what the plan did not have: the two sweeps return the caller's own type, so no call site needed a cast. **M3 THEN REFUSED THE SPLIT THE ROW NAMED**, on a measurement. The flight half is 158 lines of body and `brainFly` is 101 of them; `brainFly`, `attack` and `pursue` each steer, throttle, advance and pull a trigger, and each returns a `FireEvent`, so they are decision loops rather than steering primitives. The true primitives are 21 lines, and the behaviour half reaches the transform, the flight stats and the nine scratch vectors 69 times. Both of the plan's shapes fail on that one fact. What did leave is the shared maths: `steerQuatToward` and `velocityOf` are `game/flight-maths.ts`, read by five files outside the ships, three of which imported a 1,566-line class file to reach a helper of four. `test/constants.test.ts` caught the move in both directions, and docs/TODO/90's recorded rule chose the fix: a `THREE.Vector3` is mutable, so each file keeps its own `ZERO`. **M4 measured again and stopped**, which is docs/TODO/150 M6's precedent. `NpcState` is the one candidate left and the recommendation is to leave it, because most of its 184 lines are the doc comment beside each field. The two biggest members left — `update` at 154 and `updateTrader` at 73 — are on no candidate list, and each needs a measurement before it gets a plan. Two stale claims came out of the work: `law.ts` named `npc.ts` twice for a rule that moved, and `approach` is exported with no reader outside its own file, which is reported rather than changed. All five probes are byte-identical to the M1 baseline at every milestone. 4,752 assertions.

- [x] 173 — [The burst carries on from the pirate into the police ship](173-the-burst-carries-on-from-the-pirate-into-the-police-ship.md) — GitHub #35. Chris flew it and reported two sentences: *"I was attacked by a pirate and a police ship was there. I destroyed the pirate and the police started attacking me."* The triage of 2026-08-16 named two candidate routes and measured neither, so **the investigation ran before the plan**. **Route 1 is the live route, and the burst is what walks it.** A pirate 500 units off the nose died at frame 525 of a held burst, and the beam reached the Viper 1,400 units directly behind it at frame 540. Fifteen frames is 0.25 seconds, which is one pulse-laser cooldown: `traceShot` skips a dead ship at once and `Combat.wreck` despawns the hull in the same frame, so nothing stands between the beam and whatever was behind the target. **Route 2 needs no work**, because the station fleet only launches once route 1 has moved the record, and it announces itself. **The console could say NOTHING AT ALL**: `raiseLegal` speaks only when the record MOVES and `callStationDefence` latches after the first launch, so an Offender near a station whose fleet is already out read nothing, and the one line that did appear named the wrong pursuer. **M1 gave the console the line it never had.** `harmVerdict` (game/law.ts) is the rule and `HARM_LINES` (constants/law.ts) is the vocabulary; the covered role set comes from `offenceFor`, which is `recordVerdict`'s own shape. **The trigger is the latch and needed no new state**: `combat.ts` reads `provokedByPlayer` before the hit and after it, so a false-to-true move is the first shot that ship took, and the line is said once per ship. **A hit that DESTROYS the ship says nothing, and the plan did not have that** — a dead ship comes for nobody. The measured order is docs/TODO/130's: the deed, then the fleet, then the record. **M2 gives the fireball the rest of the burst.** For `WRECK_BURST_GRACE` seconds after her own kill, the commander's beam registers nothing on a bystander, and `inTheFireball` turns the hit into a MISS before anything reads it, so the beam, the flash, the offence and the damage all agree. `isHostileToPlayer` is the one home of "already in the fight" and this restates none of it. **THE NUMBER IS DERIVED AND THE PLAN'S PREDICTION WAS WRONG.** Over 593 seeded kills with the grace off, 12 melees turned a Viper hostile; eleven came AFTER the kill, every one between 0.25 and 1.00 seconds of it, and the twelfth came before it. The span leaves 1 of 593, 1 of 200 and 1 of 40, which is that twelfth — the plan promised 0 of 200, and **a span cannot do better than the trigger release it bets on**. **THE STATION TRUCE WIDENS THE COVER, AND THE PLAN DID NOT HAVE IT**: inside `STATION_TRUCE` an unprovoked pirate is not hostile, so it is a bystander too, which follows the truce rather than widening the rule. Out at the witchpoint a queue of pirates costs nothing at all — the second of two died 4.38 seconds after the first with the span off and on alike — and near the port the same measurement reads 4.38 against 5.13. **A shipped fixture caught that**: `test/snapshot-migrate.test.ts` kills five pirates half a second apart and two were absorbed, so its fixture paces past the grace now; the rule it pins is that five kills take a rung, not the cadence they arrive at. **`ShipSystems` needed no migration**, because `Persistence.restore` assigns onto a `freshSystems()`, so a version 3 save keeps the 0 rather than restoring `undefined` — docs/TODO/167's NaN trap answered by a structure. **`combat.ts` crossed 400 lines and an exemption was the dishonest answer**, because the file CAN be a parent plus children: `destroy` and `wreck` are `combat-wreck.ts` now, 155 lines against 343 left behind, with two delegators keeping all four outside call sites. **Ten unrelated constants gained `@rule` ids**, which is docs/TODO/160's situation again — the value 1 is shared eleven ways and only three carried one. **`test/lawful-hit.test.ts` is 37 assertions through the real Game, proved able to fail three ways, each alone.** **THE BREAK-IT STEP FOUND A VACUOUS CLAIM**: the held-burst loop first took its length from the span, so at a span of zero it fired no shots and passed. It holds half a second by hand now, because the length of a burst is a fact about a pilot. `survivability` and `aim-probe` are byte-identical; **`defence-probe` is NOT evidence and the plan was wrong to name it**, because it loads no trained weights and cannot fly the shipped defence. Two things are reported and not fixed: `recordVerdict` still names the record's pursuer rather than the ship with the grudge, and `provokedByPlayer` still never clears, so a fine does not call off a Viper. Both are docs/TODO/158's design. 4,790 assertions.

- [x] 174 — [The record holds five defects that nothing schedules](174-the-record-holds-five-defects-that-nothing-schedules.md) — five landed items each reported a defect and each declined to fix it, which was the right call every time; `tools/internal-claims.mjs` states that rule for one of them. **Nothing scheduled the second pass**, so Chris asked for all five on 2026-08-17. **Four were real, and the fifth does not exist.** **M1's novella line had moved and the plan's number was stale** — it is `novella.html:315`, not 313 — and the paragraph opens *"Commanders of established reputation"* before saying *"attempts on your name"*, so it contradicted itself across one clause; one word changed and the page keeps Chris's voice. **`HERMIT_SCATTER`'s comment claimed 2.5x the asteroid field's nominal radius and the measure is 2.8**, and **it names `ASTEROID_SCATTER` now, which the plan did not ask for**: docs/TODO/170 gave an arrival a second rock anchor, so *"the asteroid field"* had two readings, and the hermit and `ASTEROID_SCATTER` both anchor to the station while `ASTEROID_LANE_SCATTER` does not. Neither number moved — docs/TODO/170 decided that — and `npm run generate:constants` ran first because a doc comment in `src/constants/` is the catalogue's `Purpose` column. **M2 MEASURED docs/TODO/81'S UNMEASURED CLAIM, AND IT HOLDS**: no live caller of `brainName` can pass a sentinel, over both callers — `ui/screens-trainer.ts` prints `ExerciseSetup.coPilot`, which `combat-sim.ts` sets from `defenceBrainNameFor`, and `game/screens/combat-sim-setup.ts` prints the PIRATES FLY row, whose value walks `PIRATE_CHOICES`; each yields a `BrainName`, and `SimDraft` lives in one private field that nothing saves. So `AS_SHIPPED`, `AS_THE_GAME_FLIES` and `SENTINEL_NAMES` are gone, `brainName` is one lookup, and `approach` lost its `export` and gained the doc comment it never had. **A THIRD STALE CLAIM CAME OUT OF IT AND THE PLAN DID NOT HAVE IT**: `BRAINS`'s doc comment said the two pickers offer one sentinel each and that their lines live in `screens/combat-sim-notes.ts`, and both halves are false — `brainNote` reads `brainCharacter` alone, so a sentinel had no line there at all, and one picker is left. **`test/deleted-members.test.ts` is 10 assertions in two scans and each scan carries a control**, because a scan can go green on a pattern that matches nothing; **proved able to fail twice, each one alone**, and the allowlist scan went 233 constants outside the home to 230, exactly the number deleted. **M3'S DEFECT IS NOT THERE AND THE RULE STAYS.** `#screen .hints span` was measured in Chrome at viewports of 1289, 760 and 500 pixels: `#screen` sets `min-width: 640px`, so the hints container is 578 pixels at a FLOOR rather than at a reading, and the longest hint in the tree — the compare screen's `THE OTHER RECORD (n IN THE RING)` — is 319 pixels, which is 55% of it. Every hint string in the five call sites was measured in the live container at the real font. **#29's geometry is absent**: `#message` held one whole sentence in one `nowrap` element, centred on `left: 50%`, with no width floor, while a hint is a short item and `.hints` wraps normally BETWEEN items. **At 500 pixels the PANEL hangs off both edges and every row goes with it**, which is `min-width: 640px` and not this rule. **THE MEASUREMENT FOUND THE OPPOSITE DEFECT**: the station menu's own key line is a bare `.keyline` with no spans and no `nowrap`, and it breaks `S COMMANDER FILE` across two lines — the exact ugliness this rule prevents — and **it is reported rather than fixed**, because a sixth defect folded into a milestone is how the first five got lost. The record lives beside the rule in `src/style.css`, and **two of the four fixes have no gate, which the outcome states rather than implies**: the novella has none, and `tools/ste.mjs` walks `.ts`, `.js` and `.mjs` alone, so it never reads a stylesheet comment. 4,799 assertions.

- [x] 176 — [The largest files need a measurement before a cut](176-the-largest-files-need-a-measurement-before-a-cut.md) — Chris asked for the programme on 2026-08-17: *"we should pick up the large files and start breaking them down."* **`npm run sizes` read 30 files over 400 lines, and one of them was a recorded debt.** The other nine each argue they are one file, and nothing rechecks one of those arguments. **M1 MEASURED THE TWO MEMBERS docs/TODO/169 M4 NAMED, and the two answer differently.** `update` is 154 lines and reaches `this.*` 81 times over 21 members, and 19 of the 81 are calls to eight of the class's own methods; it is also the file's stated responsibility, so a caller needs the whole ship. `updateTrader` is 73 lines and 44 reaches over 7 members, and four handles answer all of them. **THE PLAN DID NOT HAVE THE STRONGEST ARGUMENT**: `tmpMat` and `tmpQ` are read at three lines of the whole file, and all three are inside that one member, while the header claims nine scratch vectors for the allocation rule. The seam is 0.60 reaches per line of subject, against the 3.3 that 169 M3 refused. **M2 TOOK IT, and `game/trader-flight.ts` is 176 lines against 1,468 left behind.** `stepTrader` takes `TraderShip` and `TraderWorld`, which are narrow interfaces rather than the class, so `NpcShip` and `WorldView` satisfy them with no cast. **Two private fields became public, and the plan did not have that**: a TypeScript class with a `private` member cannot satisfy a structural interface that names it, so `maxSpeed` and `turnRate` join `accel` and `radius`. **`approach` moved to `game/flight-maths.ts`** because four of its eight call sites went with the trader, and **docs/TODO/174 M2 took that export off under a rule that now points the other way** — a reader exists, so `test/deleted-members.test.ts` holds the new claim of one declaration and no private copy. **A SECOND STALE CLAIM CAME OUT OF IT**: `flight-maths.ts` named the five files that keep their own `ZERO`, and `player.ts` holds none. `test/trader-flight.test.ts` is 16 assertions in two parts, **proved able to fail four ways in two pairs**, and all seven probes are byte-identical to the M1 commit — `dock-traffic` and `dock-probe` joined the set because none of 169's five flies a trader. **M3 RECHECKED THE NINE STATED REASONS AND ONE IS FALSE.** `test/world-step.test.ts`'s row said it holds the step's five phases in the order they run; `world-step.ts` does hold five, and the test file holds six sections by SUBJECT whose last one is the save. **`game/combat-sim.ts` carried a number beside the sentence that retired it** — 42% safety prose, next to a clause saying the argument moved out — and measured, 356 of 934 lines are a comment of any kind and 9 name safety. `combat-sim-report.ts` is 42% report shape and `test/campaign.ts` is 26% report writer, and neither row named that half. **THE ROW LENGTH PREDICTS THE ROW QUALITY**: the nine ran 68 to 1,861 characters, not one of the four shortest is clean, and every one of the five longest states a reason that holds. **M4 GAVE THE COMBAT TRAINER A SECTION IN `docs/ARCHITECTURE.md`**, shaped the way docs/TODO/166 shaped `game.ts`, and `npm run map:report` went 28 of 56 modules unnamed to 21 of 56. **The plan counted seven modules and there are ten**, because the three it missed are each under 200 lines. **The item stops there, on 150 M6's and 169 M4's precedent**: three candidates are named and none is taken. `combat-sim-report.ts`'s report shape is the strongest — 30 files import it, 13 reach for a type alone, and 8 of the 13 are in `src/`. **`combat-sim.ts` is not a candidate, and it was measured**: 593 lines of member bodies over about twenty members, and the largest is 60. 4,818 assertions.

<!-- append-completed-todos-here -->

Supporting records: [Elite-A alignment plan](ELITE-A-COMBAT-PLAN.md),
[the survey counts every constant outside the constants directory](90-the-survey-counts-every-constant-outside-the-constants-directory.md), and
[the cleanup list tracks every constant still out of its home](90-the-cleanup-list-tracks-every-constant-still-out-of-its-home.md).
